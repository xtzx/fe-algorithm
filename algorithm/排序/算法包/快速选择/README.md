# 快速选择 (Quick Select)

> 基于快排分区：O(n) 平均时间找第 K 大/小元素

## 📦 快速使用

```typescript
import { quickSelect, topKSmallest, topKLargest, median, meta } from './src/index';
import { numberAsc } from '../公共库/src/比较器';

// 找第 K 小的元素（0-indexed）
const arr = [3, 1, 4, 1, 5, 9, 2, 6];
const kth = quickSelect([...arr], 2, numberAsc);
// 第 3 小的元素（索引 2）

// TopK：最小的 3 个元素（无序）
const smallest3 = topKSmallest(arr, 3, numberAsc);

// TopK：最大的 3 个元素（无序）
const largest3 = topKLargest(arr, 3, numberAsc);

// 中位数
const med = median(arr);
```

## 🔧 API

### `quickSelect<T>(arr, k, cmp): T`
找到第 k 小的元素（k 从 0 开始）。**注意：会修改原数组**。

### `topKSmallest<T>(arr, k, cmp): T[]`
返回最小的 k 个元素（无序，不修改原数组）。

### `topKLargest<T>(arr, k, cmp): T[]`
返回最大的 k 个元素（无序，不修改原数组）。

### `median(arr): number`
计算中位数。

### `percentile(arr, p): number`
计算百分位数（p 在 0-1 之间）。

### `meta`
算法元信息。

## 📊 复杂度

| 指标 | 值 |
|------|-----|
| 时间（平均） | O(n) |
| 时间（最坏） | O(n²) ⚠️ |
| 空间 | O(1) |

## 🆚 TopK 解法对比

| 解法 | 时间 | 空间 | 有序 | 适用场景 |
|------|------|------|------|----------|
| 全排序 | O(n log n) | O(1)~O(n) | ✅ | 简单实现 |
| 堆 | O(n log k) | O(k) | ✅ | 在线/流式 |
| **快速选择** | O(n) | O(1) | ❌ | 离线批量 |

## 📁 文件结构

```
快速选择/
├── README.md
├── src/
│   ├── index.ts    # 核心实现
│   └── demo.ts     # 使用示例
└── test/
    └── index.test.ts
```

