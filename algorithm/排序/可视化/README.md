# 排序算法可视化

> 通过动画直观理解排序算法的执行过程

## 🚀 快速开始

### 方式 1: 直接打开

```bash
# macOS
open index.html

# Windows
start index.html

# Linux
xdg-open index.html
```

### 方式 2: 本地服务器（推荐）

```bash
# 使用 npx serve
npx serve .

# 或使用 Python
python -m http.server 8080

# 访问 http://localhost:8080
```

## 📁 目录结构

```
可视化/
├── README.md
├── index.html          # 入口页面
├── styles.css          # 样式
└── src/
    ├── visualizer.ts   # 核心可视化引擎
    ├── renderer.ts     # Canvas 渲染器
    ├── controls.ts     # 控制面板
    └── algorithms/     # 各算法可视化版本
        ├── bubble.ts
        ├── quick.ts
        ├── merge.ts
        └── heap.ts
```

## 🎮 功能

- **算法选择**：冒泡、快排、归并、堆排
- **数据控制**：规模（10-200）、分布（随机/有序/逆序/重复）
- **播放控制**：播放、暂停、步进、重置
- **速度调节**：0.5x - 4x
- **统计信息**：比较次数、交换次数、耗时

## ⌨️ 快捷键

| 按键 | 功能 |
|------|------|
| `Space` | 播放/暂停 |
| `→` | 步进 |
| `R` | 重置 |
| `+` | 加速 |
| `-` | 减速 |

## 🎨 颜色说明

| 颜色 | 含义 |
|------|------|
| 🟦 #4A90D9 | 默认 |
| 🟨 #F5A623 | 比较中 |
| 🟥 #D0021B | 交换中 |
| 🟩 #7ED321 | 已排序 |
| 🟪 #9013FE | Pivot |

## 🔧 开发

### 编译 TypeScript

```bash
# 安装依赖
npm install -g typescript

# 编译
tsc src/*.ts src/algorithms/*.ts --outDir dist --module ES2020 --target ES2020
```

### 修改算法

每个算法文件都导出一个生成器函数：

```typescript
export function* bubbleSortSteps(arr: number[]): Generator<SortStep> {
  // yield 每一步操作
  yield { type: 'compare', indices: [i, j] };
  yield { type: 'swap', indices: [i, j] };
  yield { type: 'sorted', indices: [n - 1] };
}
```

