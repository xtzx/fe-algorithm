/**
 * ============================================================
 * 📚 前置知识 - 速查表汇总
 * ============================================================
 *
 * 本文件汇总了所有重要的速查表，面试前快速浏览。
 */

// ============================================================
// 1. 📊 复杂度速查表
// ============================================================

/**
 * 【常见复杂度对比】
 *
 * ┌────────────┬──────────────┬────────────────────────────────┐
 * │  复杂度     │  n=1000      │  典型算法                       │
 * ├────────────┼──────────────┼────────────────────────────────┤
 * │  O(1)      │  1           │  数组访问、哈希表查找            │
 * │  O(log n)  │  10          │  二分查找                       │
 * │  O(n)      │  1,000       │  线性遍历                       │
 * │  O(n log n)│  10,000      │  归并排序、快排                  │
 * │  O(n²)     │  1,000,000   │  冒泡排序、双重循环              │
 * │  O(2^n)    │  天文数字     │  子集枚举、回溯                  │
 * │  O(n!)     │  更大         │  全排列                         │
 * └────────────┴──────────────┴────────────────────────────────┘
 *
 * 【数据规模 → 可接受复杂度】
 *
 * ┌────────────────┬─────────────────────────────────────────┐
 * │  数据规模 n     │  可接受的复杂度                         │
 * ├────────────────┼─────────────────────────────────────────┤
 * │  n ≤ 20        │  O(2^n)、O(n!)                          │
 * │  n ≤ 100       │  O(n³)                                  │
 * │  n ≤ 1,000     │  O(n²)                                  │
 * │  n ≤ 10^5      │  O(n log n)、O(n)                       │
 * │  n ≤ 10^6      │  O(n)                                   │
 * │  n > 10^6      │  O(log n)、O(1)                         │
 * └────────────────┴─────────────────────────────────────────┘
 */

// ============================================================
// 2. 🎯 信号词 → 算法映射速查表 ⭐⭐⭐
// ============================================================

/**
 * 【最重要的速查表！看到关键词立刻联想算法】
 *
 * ┌─────────────────────────────────────────────────────────────────────────┐
 * │                          信号词 → 算法                                   │
 * ├────────────────────────┬────────────────────┬───────────────────────────┤
 * │ 关键词                  │ 算法               │ 典型题目                   │
 * ├────────────────────────┼────────────────────┼───────────────────────────┤
 * │ 连续子数组/子串         │ 滑动窗口           │ 无重复最长子串              │
 * │ 有序 + 找两个数        │ 双指针             │ 两数之和 II                │
 * │ 原地去重/移动          │ 快慢指针           │ 移动零                     │
 * │ 和为 K / 前缀相关      │ 前缀和 + 哈希      │ 和为K的子数组              │
 * │ 下一个更大/更小        │ 单调栈             │ 每日温度                   │
 * │ 滑动窗口最值           │ 单调队列           │ 滑动窗口最大值              │
 * │ 所有可能/所有方案      │ 回溯              │ 全排列、子集                │
 * │ 最长/最短/最大/最小    │ DP                │ LIS、最大子数组和           │
 * │ 多少种方法/方案数      │ DP                │ 爬楼梯、零钱兑换II          │
 * │ 是否可能/能否达到      │ DP / BFS          │ 单词拆分                   │
 * │ 选或不选/容量限制      │ 背包 DP           │ 0-1背包、分割等和           │
 * │ 连通性/岛屿            │ DFS/BFS/并查集    │ 岛屿数量                   │
 * │ 依赖关系/顺序          │ 拓扑排序          │ 课程表                     │
 * │ 第 K 大/小            │ 堆/快速选择       │ 数组中第K大元素             │
 * │ 前缀匹配              │ 字典树            │ 实现Trie                   │
 * │ 有序 + 查找           │ 二分查找          │ 搜索插入位置                │
 * │ 最大化最小/最小化最大  │ 答案二分          │ 分割数组最大值              │
 * └────────────────────────┴────────────────────┴───────────────────────────┘
 */

// ============================================================
// 3. 💻 位运算速查表
// ============================================================

/**
 * ┌─────────────────────────────────────────────────────────────┐
 * │                      位运算速查表                            │
 * ├─────────────────────┬───────────────────────────────────────┤
 * │ 操作                 │ 代码                                 │
 * ├─────────────────────┼───────────────────────────────────────┤
 * │ 判断奇偶             │ n & 1                                │
 * │ 乘以 2               │ n << 1                               │
 * │ 除以 2               │ n >> 1                               │
 * │ 去掉最低位的 1       │ n & (n - 1)                          │
 * │ 获取最低位的 1       │ n & -n                               │
 * │ 判断 2 的幂          │ n > 0 && (n & (n-1)) === 0           │
 * │ 第 i 位置 1         │ n | (1 << i)                         │
 * │ 第 i 位置 0         │ n & ~(1 << i)                        │
 * │ 检查第 i 位         │ (n >> i) & 1                         │
 * │ 翻转第 i 位         │ n ^ (1 << i)                         │
 * │ 交换两数            │ a^=b; b^=a; a^=b;                    │
 * └─────────────────────┴───────────────────────────────────────┘
 */

// ============================================================
// 4. 📝 边界情况速查表
// ============================================================

/**
 * ┌─────────────────────────────────────────────────────────────┐
 * │                      边界情况速查表                          │
 * ├─────────────────────┬───────────────────────────────────────┤
 * │ 数据类型             │ 需要检查的边界                        │
 * ├─────────────────────┼───────────────────────────────────────┤
 * │ 数组                 │ 空数组、单元素、全相同、重复元素       │
 * │ 字符串               │ 空串、单字符、全相同                   │
 * │ 链表                 │ 空、单节点、有环                       │
 * │ 树                   │ 空、只有根、退化成链表                 │
 * │ 数值                 │ 0、负数、最大/最小整数、溢出           │
 * │ 二分                 │ 目标在边界、目标不存在                 │
 * │ DP                   │ dp[0] 初始化、下标越界                 │
 * └─────────────────────┴───────────────────────────────────────┘
 */

// ============================================================
// 5. 🔄 DP 分类速查表
// ============================================================

/**
 * ┌─────────────────────────────────────────────────────────────────────────┐
 * │                          DP 分类速查表                                   │
 * ├─────────────────┬────────────────────────┬──────────────────────────────┤
 * │ 类型             │ 状态定义               │ 典型题目                      │
 * ├─────────────────┼────────────────────────┼──────────────────────────────┤
 * │ 线性 DP         │ dp[i] = 以i结尾/前i个  │ LIS、打家劫舍                 │
 * │ 二维 DP         │ dp[i][j] = 两序列/网格 │ LCS、编辑距离、不同路径       │
 * │ 0-1 背包        │ dp[j] = 容量j的最优解  │ 分割等和子集（内层从后往前）   │
 * │ 完全背包        │ dp[j] = 容量j的最优解  │ 零钱兑换（内层从前往后）       │
 * │ 区间 DP         │ dp[i][j] = 区间[i,j]   │ 戳气球（枚举分割点）          │
 * │ 状态机 DP       │ dp[i][state]          │ 买卖股票系列                  │
 * └─────────────────┴────────────────────────┴──────────────────────────────┘
 */

// ============================================================
// 6. 🎤 面试沟通模板
// ============================================================

/**
 * ┌─────────────────────────────────────────────────────────────┐
 * │                      面试沟通模板                            │
 * ├─────────────────────────────────────────────────────────────┤
 * │                                                             │
 * │ 【理解题意】                                                  │
 * │ "让我确认一下：输入是...，输出是...，对吗？"                  │
 * │ "请问数据规模大概是多少？"                                    │
 * │ "边界情况怎么处理？比如空数组..."                             │
 * │                                                             │
 * │ 【思考解法】                                                  │
 * │ "我先想一个暴力解法..."                                      │
 * │ "暴力解法复杂度是 O(n²)，让我想想优化..."                    │
 * │ "这道题让我想到了 XX，可能可以用..."                          │
 * │                                                             │
 * │ 【编写代码】                                                  │
 * │ "我先写个框架..."                                            │
 * │ "这里用双指针是因为..."                                      │
 * │ "这个边界需要特殊处理..."                                    │
 * │                                                             │
 * │ 【测试代码】                                                  │
 * │ "让我用这个例子走一遍..."                                    │
 * │ "这里有个小问题，我来修复..."                                 │
 * │                                                             │
 * │ 【总结】                                                      │
 * │ "时间复杂度 O(n)，空间复杂度 O(1)..."                        │
 * │ "如果要进一步优化，可以考虑..."                              │
 * │                                                             │
 * └─────────────────────────────────────────────────────────────┘
 */

// ============================================================
// 7. ⏰ 时间分配速查
// ============================================================

/**
 * 【45 分钟面试时间分配】
 *
 * ┌────────────────┬─────────┬────────────────────────────────┐
 * │ 阶段            │ 时间    │ 要点                           │
 * ├────────────────┼─────────┼────────────────────────────────┤
 * │ 理解题意        │ 5 分钟  │ 复述、确认边界、看数据规模      │
 * │ 思考解法        │ 5 分钟  │ 暴力→优化，确认后再写          │
 * │ 编写代码        │ 20 分钟 │ 边写边说，先框架后细节          │
 * │ 测试调试        │ 10 分钟 │ 用例走查，修复 bug             │
 * │ 总结讨论        │ 5 分钟  │ 复杂度分析，提及其他解法        │
 * └────────────────┴─────────┴────────────────────────────────┘
 */

// ============================================================
// 8. ⚠️ 常见错误速查
// ============================================================

/**
 * ┌─────────────────────────────────────────────────────────────┐
 * │                      常见错误速查                            │
 * ├─────────────────┬───────────────────────────────────────────┤
 * │ 错误类型         │ 检查点                                   │
 * ├─────────────────┼───────────────────────────────────────────┤
 * │ 下标越界        │ i-1, i+1 是否合法？循环条件 < 还是 <=？   │
 * │ 循环不终止      │ 循环变量更新了吗？二分 left=mid+1?       │
 * │ 初始化错误      │ 最大取 -∞，最小取 ∞，计数 dp[0]=1?      │
 * │ 返回值错误      │ 下标还是值？无解返回什么？max(dp)?       │
 * │ 引用问题        │ [...arr] 拷贝了吗？                      │
 * │ 遍历顺序        │ 0-1背包从后往前？依赖的状态算了吗？       │
 * └─────────────────┴───────────────────────────────────────────┘
 */

// ============================================================
// 9. 📂 文件索引
// ============================================================

/**
 * ┌─────────────────────────────────────────────────────────────┐
 * │                        文件索引                             │
 * ├─────────────────────────────────────────────────────────────┤
 * │                                                             │
 * │  index.ts              ─ 前置知识总览                       │
 * │  01-complexity.ts      ─ 复杂度分析                         │
 * │  02-math-basics.ts     ─ 基础数学（位运算、排列组合等）      │
 * │  03-problem-modeling.ts ─ 问题建模思维 ⭐ 核心              │
 * │  04-methodology.ts     ─ 解题方法论                         │
 * │  05-interview-tips.ts  ─ 面试技巧                           │
 * │  summary.ts            ─ 本文件，速查表汇总                  │
 * │                                                             │
 * └─────────────────────────────────────────────────────────────┘
 */

export {};

