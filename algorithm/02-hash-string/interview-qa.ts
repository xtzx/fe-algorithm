/**
 * ============================================================
 * 📚 哈希表与字符串 - 面试高频问答
 * ============================================================
 *
 * 本文件包含哈希表与字符串相关的面试高频问题，每个问题包含：
 * 1. 问题本身
 * 2. 标准答案
 * 3. 加分回答（展示深度理解）
 * 4. 追问方向
 */

// ============================================================
// Q1: Map、Object、Set 怎么选？
// ============================================================

/**
 * 📝 问题：JavaScript 中 Map、Object、Set 有什么区别？什么时候用哪个？
 *
 * ✅ 标准答案：
 *
 * Map：
 * - 键可以是任意类型（对象、函数、数字等）
 * - 保持插入顺序
 * - 有 size 属性，直接获取大小
 * - 迭代性能好
 *
 * Object：
 * - 键只能是字符串或 Symbol
 * - 有原型链，可能有意外的键
 * - 适合作为配置对象、JSON 数据
 *
 * Set：
 * - 只存储值，不存储键值对
 * - 值唯一，自动去重
 * - 适合判断存在、去重操作
 *
 * ⭐ 加分回答：
 * 「在算法题中，我通常这样选择：
 *
 * 1. 键是数字或需要保持顺序 → Map
 *    比如两数之和，键是数字，用 Map 更语义化
 *
 * 2. 只需要判断"存在" → Set
 *    比如最长连续序列，只需要 has() 判断
 *
 * 3. 键是固定范围的小整数（如 26 个字母）→ 数组
 *    比如异位词判断，用 count[26] 比 Map 更快
 *
 * 性能方面，V8 引擎对 Object 有特殊优化，小规模数据 Object 可能更快，
 * 但 Map 在频繁增删的场景下更稳定。」
 *
 * 🔄 可能的追问：
 * - WeakMap 和 WeakSet 是什么？什么时候用？
 * - Map 和 Object 的性能差异？
 */

// ============================================================
// Q2: 两数之和为什么先查后存？
// ============================================================

/**
 * 📝 问题：两数之和中，为什么要先查找再存入？能不能先存后查？
 *
 * ✅ 标准答案：
 * 不能先存后查，因为可能会找到自己。
 *
 * 例如：nums = [3, 3], target = 6
 * - 如果先存 {3: 0}，再查 6-3=3，会找到下标 0（自己）
 * - 但题目要求两个不同位置的数
 *
 * 先查后存的顺序保证了：查找的是之前遍历过的数，不会是当前数。
 *
 * ⭐ 加分回答：
 * 「这个"先查后存"的顺序体现了一个重要思想：
 * 在遍历过程中逐步构建可查询的数据结构。
 *
 * 这个模式还用在很多题目中：
 * - 四数相加 II：先遍历 AB 构建 map，再遍历 CD 查找
 * - 和为 K 的子数组：边算前缀和，边查找 prefix - k
 *
 * 本质是把"找配对"的问题转化为"一边遍历，一边记录"，
 * 用空间换时间，把 O(n²) 降到 O(n)。」
 *
 * 🔄 可能的追问：
 * - 如果允许使用同一个元素两次呢？
 * - 如果要返回所有满足条件的配对呢？
 */

// ============================================================
// Q3: 异位词判断有几种方法？
// ============================================================

/**
 * 📝 问题：如何判断两个字符串是异位词？有哪些方法？
 *
 * ✅ 标准答案：
 *
 * 方法一：排序比较
 * - 两个字符串排序后相等则是异位词
 * - 时间 O(n log n)，空间 O(n)
 *
 * 方法二：字符计数
 * - 统计两个字符串的字符频率，相等则是异位词
 * - 时间 O(n)，空间 O(k)，k 是字符集大小
 *
 * 方法三：单 Map 抵消
 * - 第一个字符串 +1，第二个 -1，最后全为 0 则是异位词
 * - 时间 O(n)，空间 O(k)
 *
 * ⭐ 加分回答：
 * 「选择方法时要考虑具体场景：
 *
 * 1. 只判断一对：方法三最好，只需一个 Map
 *
 * 2. 异位词分组（多个字符串分组）：
 *    - 排序后的字符串作为 key → 代码简单
 *    - 字符计数转字符串作为 key → 对于长字符串更快
 *
 * 3. 滑动窗口找异位词：
 *    - 固定大小的窗口 + 字符计数
 *    - 用 valid 变量记录满足条件的字符种类数
 *
 * 对于纯小写字母，可以用 int[26] 代替 Map，性能更好。
 * key 可以是 "a1b2c3" 这样的格式，或者直接用 count.join(',')。」
 *
 * 🔄 可能的追问：
 * - 异位词分组的 key 怎么设计？
 * - 如果字符集很大（Unicode）怎么办？
 */

// ============================================================
// Q4: 最长连续序列为什么是 O(n)？
// ============================================================

/**
 * 📝 问题：最长连续序列的哈希表解法，看起来有两层循环，为什么是 O(n)？
 *
 * ✅ 标准答案：
 * 虽然代码有两层循环，但每个数最多被访问两次：
 * 1. 外层遍历一次
 * 2. 内层循环中，作为某个起点的连续序列的一部分访问一次
 *
 * 关键优化是：只从序列起点开始数。
 * 如果 num-1 存在，说明 num 不是起点，直接跳过。
 *
 * ⭐ 加分回答：
 * 「这是一个很好的"看起来 O(n²) 实际是 O(n)"的例子。
 *
 * 关键是理解"均摊复杂度"：
 * - 假设有 k 个连续序列
 * - 每个序列只会从起点开始遍历一次
 * - 总的内层循环次数 = 所有序列长度之和 = n
 *
 * 这个技巧的本质是：通过起点判断，避免重复遍历。
 *
 * 类似的均摊分析还有：
 * - 滑动窗口：left 和 right 各遍历一次
 * - 单调栈：每个元素入栈出栈各一次
 * - KMP 算法：虽然有两层循环，但是 O(n+m)」
 *
 * 🔄 可能的追问：
 * - 如果不用起点判断，直接数会怎样？
 * - 还有什么题目是"看起来 O(n²) 实际 O(n)"？
 */

// ============================================================
// Q5: 字符串哈希（Rabin-Karp）了解吗？
// ============================================================

/**
 * 📝 问题：什么是字符串哈希？Rabin-Karp 算法的原理是什么？
 *
 * ✅ 标准答案：
 * 字符串哈希是将字符串映射为一个数字（哈希值），用于快速比较。
 *
 * Rabin-Karp 算法用于字符串匹配：
 * - 计算模式串的哈希值
 * - 滑动窗口计算文本串每个位置的哈希值
 * - 哈希值相等时再逐字符比较确认
 *
 * 滚动哈希公式（以 26 进制为例）：
 * hash("abc") = a*26² + b*26 + c
 * hash("bcd") = (hash("abc") - a*26²) * 26 + d
 *
 * ⭐ 加分回答：
 * 「Rabin-Karp 的关键是滚动哈希，可以 O(1) 时间更新窗口哈希值。
 *
 * 实际应用中需要注意：
 * 1. 哈希冲突：哈希值相等不代表字符串相等，需要二次确认
 * 2. 大数溢出：需要取模，常用大质数如 10^9+7
 * 3. 双哈希：用两个不同的模数减少冲突概率
 *
 * 应用场景：
 * - 重复的 DNA 序列：找所有出现超过一次的 10 位 DNA
 * - 最长重复子串：二分 + 字符串哈希
 * - 字符串匹配（面试中 KMP 更常问）
 *
 * 平均时间复杂度 O(n+m)，最坏情况（大量冲突）O(nm)。」
 *
 * 🔄 可能的追问：
 * - KMP 和 Rabin-Karp 有什么区别？
 * - 如何处理哈希冲突？
 */

// ============================================================
// Q6: 滑动窗口中 valid 变量的作用？
// ============================================================

/**
 * 📝 问题：滑动窗口 + 哈希表的模板中，valid 变量是什么意思？
 *
 * ✅ 标准答案：
 * valid 表示"满足条件的字符种类数"。
 *
 * 例如在"找所有异位词"中：
 * - need = {a:1, b:2, c:1}，有 3 种字符
 * - 当窗口中 a 的数量 = 1 时，valid++
 * - 当窗口中 b 的数量 = 2 时，valid++
 * - 当 valid = 3（= need.size）时，说明所有字符都满足条件
 *
 * ⭐ 加分回答：
 * 「valid 的设计很巧妙，避免了每次都遍历整个 need Map 来判断是否匹配。
 *
 * 更新 valid 的时机：
 * - 扩张时：window[c] 从 need[c]-1 变成 need[c] 时，valid++
 * - 收缩时：window[c] 从 need[c] 变成 need[c]-1 时，valid--
 *
 * 注意判断条件是 window[c] === need[c]，不是 >=：
 * - 因为我们统计的是"恰好满足"的字符种类数
 * - window[c] > need[c] 时反而不算满足（多了）
 *
 * 这个模式可以解决：
 * - 找所有异位词（valid === need.size）
 * - 最小覆盖子串（valid === need.size，但 window[c] >= need[c] 算满足）
 * - 字符串的排列
 *
 * 不同题目 valid 的判断条件略有不同，需要理解本质。」
 *
 * 🔄 可能的追问：
 * - 最小覆盖子串和找异位词的区别？
 * - 如何处理"至少"和"恰好"的区别？
 */

// ============================================================
// Q7: 回文问题有哪些解法？
// ============================================================

/**
 * 📝 问题：回文相关的问题有哪些解法？
 *
 * ✅ 标准答案：
 *
 * 1. 判断回文串：双指针 O(n)
 *
 * 2. 最长回文子串：
 *    - 中心扩展法 O(n²)，推荐
 *    - 动态规划 O(n²)
 *    - Manacher O(n)，面试少考
 *
 * 3. 回文子串个数：
 *    - 中心扩展，枚举每个中心
 *
 * 4. 分割回文串：
 *    - 回溯 + 预处理 dp[i][j] 是否回文
 *
 * ⭐ 加分回答：
 * 「中心扩展是最常用的，因为代码简洁且容易理解。
 *
 * 关键点：
 * 1. 回文中心可能是一个字符（奇数长度）或两个字符中间（偶数长度）
 * 2. 所以要枚举 2n-1 个中心，或者分两种情况处理
 *
 * 代码技巧：
 * function expand(s, left, right) {
 *   while (left >= 0 && right < s.length && s[left] === s[right]) {
 *     left--;
 *     right++;
 *   }
 *   return right - left - 1; // 回文长度
 * }
 *
 * 对于每个位置 i：
 * - 奇数回文：expand(s, i, i)
 * - 偶数回文：expand(s, i, i+1)
 *
 * Manacher 算法虽然是 O(n)，但面试中很少要求写出来，
 * 知道原理（利用回文对称性跳过已知部分）即可。」
 *
 * 🔄 可能的追问：
 * - 最长回文子序列呢？（动态规划）
 * - 回文对怎么做？
 */

// ============================================================
// Q8: 大数运算怎么实现？
// ============================================================

/**
 * 📝 问题：如何实现大数相加、相乘？
 *
 * ✅ 标准答案：
 *
 * 大数相加：
 * - 从右往左逐位相加
 * - 处理进位 carry
 * - 结果可能比原数多一位
 *
 * 大数相乘：
 * - 模拟竖式乘法
 * - result[i+j] += a[i] * b[j]
 * - 最后统一处理进位
 *
 * ⭐ 加分回答：
 * 「大数相乘的关键是理解位置关系：
 *
 * 设 a 长度为 m，b 长度为 n
 * - a[i] * b[j] 的结果应该加到 result[i+j] 和 result[i+j+1]
 * - 结果长度最多是 m + n
 *
 * 代码技巧：
 * 1. 先不处理进位，最后统一处理
 * 2. 使用数组存储每一位，方便操作
 * 3. 去除前导零
 *
 * 进阶算法（面试一般不考）：
 * - Karatsuba：O(n^1.585)，分治思想
 * - FFT：O(n log n)，快速傅里叶变换
 *
 * 前端场景：
 * - BigInt 类型可以处理大整数
 * - 但面试时通常要求手写实现」
 *
 * 🔄 可能的追问：
 * - JavaScript 的 BigInt 了解吗？
 * - 大数除法怎么实现？
 */

// ============================================================
// Q9: 哈希表的底层原理？
// ============================================================

/**
 * 📝 问题：哈希表的底层原理是什么？如何处理冲突？
 *
 * ✅ 标准答案：
 *
 * 底层原理：
 * 1. 哈希函数：将键映射为数组下标
 * 2. 数组存储：根据下标存取值
 *
 * 冲突处理：
 * 1. 链地址法：相同下标的元素用链表连接
 * 2. 开放地址法：冲突时探测下一个空位
 *    - 线性探测：+1, +2, +3...
 *    - 二次探测：+1, +4, +9...
 *    - 双重哈希：用第二个哈希函数
 *
 * ⭐ 加分回答：
 * 「JavaScript 的 Map 内部实现：
 *
 * V8 引擎对 Map 使用了一种叫"有序哈希表"的结构：
 * - 保持插入顺序（ES6 规范要求）
 * - 使用链地址法处理冲突
 * - 自动扩容（负载因子约 0.75）
 *
 * 为什么说哈希表最坏是 O(n)？
 * - 如果所有键都冲突到同一个桶，查找变成链表遍历
 * - 好的哈希函数可以让冲突均匀分布
 *
 * Object vs Map 的性能差异：
 * - Object：V8 有 hidden class 优化，小规模可能更快
 * - Map：大规模数据、频繁增删更稳定
 *
 * 实际面试中，默认哈希表操作是 O(1) 即可。」
 *
 * 🔄 可能的追问：
 * - 什么是负载因子？
 * - 扩容时如何处理？（重新哈希）
 */

// ============================================================
// Q10: 字符串不可变性的影响？
// ============================================================

/**
 * 📝 问题：JavaScript 中字符串是不可变的，这对算法有什么影响？
 *
 * ✅ 标准答案：
 *
 * 字符串不可变意味着：
 * - 不能修改字符串中的某个字符
 * - 每次"修改"都会创建新字符串
 *
 * 影响：
 * 1. 字符串拼接：s += c 是 O(n) 而不是 O(1)
 * 2. 反转字符串：需要先转数组
 * 3. 原地修改：不可能，必须用数组
 *
 * ⭐ 加分回答：
 * 「正确处理方式：
 *
 * 1. 大量拼接用数组：
 *    const arr = [];
 *    arr.push(c);
 *    return arr.join('');
 *
 * 2. 需要修改用数组：
 *    const chars = s.split('');
 *    // 修改 chars
 *    return chars.join('');
 *
 * 3. 反转字符串：
 *    s.split('').reverse().join('');
 *
 * 复杂度分析：
 * - n 次 s += c → O(n²)
 * - n 次 arr.push + 一次 join → O(n)
 *
 * Python 也有同样的问题，而 C++/Java 的 StringBuilder 就是为此设计的。
 *
 * 实际写算法时，如果需要频繁修改字符串，一开始就用数组处理，
 * 最后再 join 成字符串。」
 *
 * 🔄 可能的追问：
 * - 模板字符串的性能呢？
 * - String.prototype 上有哪些常用方法？
 */

// ============================================================
// 📋 面试问答速查表
// ============================================================

/**
 * ┌─────────────────────────────────────────────────────────────────────────┐
 * │                           面试问答速查表                                 │
 * ├────────────────────────────────┬────────────────────────────────────────┤
 * │           问题类型             │              核心答案要点               │
 * ├────────────────────────────────┼────────────────────────────────────────┤
 * │ Map vs Object vs Set          │ Map任意键+有序，Set去重，Object配置     │
 * │ 两数之和先查后存               │ 避免找到自己，边遍历边构建              │
 * │ 异位词判断方法                 │ 排序 O(nlogn)、计数 O(n)、抵消法        │
 * │ 最长连续序列 O(n)             │ 只从起点开始数，均摊分析                 │
 * │ Rabin-Karp 原理               │ 滚动哈希，O(1) 更新窗口哈希值            │
 * │ 滑动窗口 valid                │ 满足条件的字符种类数，避免遍历比较        │
 * │ 回文问题解法                  │ 中心扩展（常用）、DP、Manacher           │
 * │ 大数运算                      │ 逐位计算、处理进位、注意位置关系          │
 * │ 哈希表原理                    │ 哈希函数 + 数组，链地址法/开放地址法      │
 * │ 字符串不可变                  │ 用数组处理，最后 join，避免 O(n²) 拼接   │
 * └────────────────────────────────┴────────────────────────────────────────┘
 */

// ============================================================
// 🎯 回答问题的技巧
// ============================================================

/**
 * 哈希表/字符串题目的回答模板：
 *
 * 1. 确认输入输出：
 *    「这道题输入是...，输出是...，有没有特殊情况需要处理？」
 *
 * 2. 分析问题类型：
 *    「这是一个找配对/统计频率/子串匹配的问题」
 *
 * 3. 说明核心思路：
 *    「我用哈希表存储...，遍历时查找...」
 *
 * 4. 代码实现要点：
 *    「关键是...的顺序/...的更新条件」
 *
 * 5. 复杂度分析：
 *    「时间 O(n)，因为...；空间 O(k)，k 是...」
 *
 * 示例回答：
 * 「这道题是在无序数组中找两个数和为 target，是典型的"找配对"问题。
 * 我用哈希表存储已遍历的数和下标，对于当前数 x，查找 target-x 是否存在。
 * 关键是"先查后存"的顺序，避免找到自己。
 * 时间 O(n)，每个数访问一次；空间 O(n)，最坏情况存所有数。」
 */

export {};

