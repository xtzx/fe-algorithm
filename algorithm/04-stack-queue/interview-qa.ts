/**
 * ============================================================
 * 📚 栈与队列 - 面试高频问答
 * ============================================================
 *
 * 本文件包含栈与队列相关的面试高频问题，每个问题包含：
 * 1. 问题本身
 * 2. 标准答案
 * 3. 加分回答（展示深度理解）
 * 4. 追问方向
 */

// ============================================================
// Q1: 栈和队列的区别？
// ============================================================

/**
 * 📝 问题：栈和队列有什么区别？什么时候用栈，什么时候用队列？
 *
 * ✅ 标准答案：
 *
 * 栈（Stack）：
 * - 后进先出（LIFO）
 * - 只能从栈顶操作
 * - 典型场景：括号匹配、表达式求值、递归模拟
 *
 * 队列（Queue）：
 * - 先进先出（FIFO）
 * - 从尾部入队，从头部出队
 * - 典型场景：BFS、任务调度、消息队列
 *
 * ⭐ 加分回答：
 * 「选择栈还是队列，主要看处理顺序：
 *
 * 用栈的信号：
 * - 需要"匹配最近的"（括号匹配）
 * - 需要"后进先出"（撤销操作）
 * - 递归转迭代（DFS）
 * - 保存和恢复状态
 *
 * 用队列的信号：
 * - 需要"先进先出"（任务调度）
 * - 层序遍历（BFS）
 * - 滑动窗口（单调队列）
 *
 * JavaScript 实现：
 * - 栈：直接用数组，push/pop 都是 O(1)
 * - 队列：数组的 shift 是 O(n)，可以用对象或双指针优化
 *
 * 前端最常见的就是事件循环中的任务队列，
 * 宏任务队列和微任务队列都是 FIFO。」
 *
 * 🔄 可能的追问：
 * - JavaScript 中如何高效实现队列？
 * - 事件循环中的队列是怎么工作的？
 */

// ============================================================
// Q2: 什么是单调栈？怎么用？
// ============================================================

/**
 * 📝 问题：什么是单调栈？什么时候用单调栈？
 *
 * ✅ 标准答案：
 * 单调栈是一种特殊的栈，栈中元素保持单调递增或递减。
 *
 * 用途：找每个元素的"下一个更大/更小"或"上一个更大/更小"元素。
 *
 * 分类：
 * - 单调递减栈（栈底到栈顶递减）：找下一个更大
 * - 单调递增栈（栈底到栈顶递增）：找下一个更小
 *
 * ⭐ 加分回答：
 * 「单调栈的核心思想是：
 * 当新元素破坏单调性时，说明它是某些元素的"答案"。
 *
 * 例如找下一个更大元素：
 * - 用单调递减栈
 * - 当 nums[i] > 栈顶时，栈顶的下一个更大元素就是 nums[i]
 * - 弹出栈顶，记录答案
 * - 继续比较直到栈空或 nums[i] <= 栈顶
 * - 最后 nums[i] 入栈
 *
 * 为什么是 O(n)？
 * - 每个元素最多入栈一次、出栈一次
 * - 总操作次数 ≤ 2n，所以是 O(n)
 *
 * 典型题目：
 * - 每日温度：找下一个更高温度
 * - 柱状图最大矩形：找左右边界
 * - 接雨水：找左右最高点
 *
 * 记忆口诀：找大用递减，找小用递增。」
 *
 * 🔄 可能的追问：
 * - 单调栈存索引还是存值？
 * - 如何处理循环数组？
 */

// ============================================================
// Q3: 柱状图最大矩形怎么解？
// ============================================================

/**
 * 📝 问题：柱状图中最大的矩形面积怎么求？
 *
 * ✅ 标准答案：
 * 用单调递增栈，找每个柱子能向左右扩展到的边界。
 *
 * 思路：
 * 1. 对于每个柱子，找到它左边第一个更矮的位置
 * 2. 找到它右边第一个更矮的位置
 * 3. 以该柱子高度为高，左右边界间距离为宽，计算面积
 *
 * 时间复杂度：O(n)
 * 空间复杂度：O(n)
 *
 * ⭐ 加分回答：
 * 「实现的关键点：
 *
 * 1. 为什么用单调递增栈？
 *    - 找"下一个更小"的元素
 *    - 小的柱子会终止大的柱子的扩展
 *
 * 2. 边界处理技巧：
 *    - 在数组两端各加一个高度为 0 的虚拟柱子
 *    - 左边的 0 确保所有柱子都能入栈
 *    - 右边的 0 确保所有柱子都能出栈计算
 *
 * 3. 计算公式：
 *    - 高度 = heights[弹出的索引]
 *    - 宽度 = 当前索引 - 新栈顶索引 - 1
 *
 * 变体题目：
 * - 最大矩形（二维）：每行转化为柱状图
 *
 * 时间复杂度仍然是 O(n)，因为每个元素入栈出栈各一次。」
 *
 * 🔄 可能的追问：
 * - 不加哨兵怎么处理边界？
 * - 二维的最大矩形怎么做？
 */

// ============================================================
// Q4: 接雨水问题有哪些解法？
// ============================================================

/**
 * 📝 问题：接雨水问题怎么解？有几种方法？
 *
 * ✅ 标准答案：
 *
 * 方法一：动态规划
 * - 预处理每个位置左边最高和右边最高
 * - 每个位置能接的水 = min(左最高, 右最高) - 当前高度
 * - 时间 O(n)，空间 O(n)
 *
 * 方法二：双指针
 * - 左右指针相向移动
 * - 维护左边最高和右边最高
 * - 时间 O(n)，空间 O(1)
 *
 * 方法三：单调栈
 * - 单调递减栈，横向计算水量
 * - 时间 O(n)，空间 O(n)
 *
 * ⭐ 加分回答：
 * 「三种方法的思路不同：
 *
 * DP 是"竖着算"：
 * - 每个位置能装多少水 = min(左max, 右max) - height
 * - 需要预处理左右最高
 *
 * 双指针也是"竖着算"：
 * - 优化空间，不需要预处理
 * - 谁小谁动，小的那边的最大值确定
 *
 * 单调栈是"横着算"：
 * - 遇到更高的柱子时，计算凹槽的水量
 * - 类似柱状图最大矩形
 *
 * 面试中推荐双指针，因为空间 O(1)，代码也不长。
 *
 * 双指针的关键理解：
 * - 如果 left_max < right_max，左边位置能接的水只取决于 left_max
 * - 因为右边一定有更高的挡住」
 *
 * 🔄 可能的追问：
 * - 接雨水 II（二维）怎么做？
 * - 双指针为什么正确？
 */

// ============================================================
// Q5: 单调队列是什么？和单调栈有什么区别？
// ============================================================

/**
 * 📝 问题：什么是单调队列？和单调栈有什么区别？
 *
 * ✅ 标准答案：
 * 单调队列是一种双端队列，队列中元素保持单调性。
 *
 * 与单调栈的区别：
 * - 单调栈：只在一端操作，用于找下一个更大/小
 * - 单调队列：两端都可操作，用于滑动窗口最值
 *
 * 单调队列的特点：
 * - 队首始终是窗口最大（或最小）值
 * - 新元素入队时从队尾移除更小（或更大）的元素
 * - 窗口滑动时检查队首是否过期
 *
 * ⭐ 加分回答：
 * 「单调队列最经典的应用是"滑动窗口最大值"：
 *
 * 核心操作：
 * 1. 移除过期元素：队首索引 <= i - k 时移除
 * 2. 保持单调性：新元素入队前，移除所有比它小的
 * 3. 记录答案：队首就是窗口最大值
 *
 * 为什么用双端队列？
 * - 队首移除过期元素
 * - 队尾维护单调性
 *
 * 时间复杂度 O(n)：
 * - 每个元素最多入队出队各一次
 *
 * JavaScript 实现注意：
 * - 数组的 shift() 是 O(n)，大量调用会超时
 * - 可以用双指针模拟，或者用 Deque 库
 *
 * 与优先队列/堆的对比：
 * - 堆也能解决滑动窗口最值，但是 O(n log k)
 * - 单调队列是 O(n)，更优」
 *
 * 🔄 可能的追问：
 * - 滑动窗口最小值怎么做？
 * - 如何优化 JavaScript 的 shift？
 */

// ============================================================
// Q6: 如何用两个栈实现队列？
// ============================================================

/**
 * 📝 问题：如何用两个栈实现队列？时间复杂度多少？
 *
 * ✅ 标准答案：
 * 用一个入栈（in）存储入队元素，一个出栈（out）存储出队元素。
 *
 * 入队：直接 push 到 in 栈
 * 出队：如果 out 栈空，把 in 栈全部倒入 out 栈，然后 pop
 *
 * 时间复杂度：
 * - 入队 O(1)
 * - 出队 均摊 O(1)（每个元素最多倒腾一次）
 *
 * ⭐ 加分回答：
 * 「均摊分析的理解：
 *
 * 虽然单次出队可能是 O(n)（需要倒腾），
 * 但每个元素在整个生命周期中：
 * - 入 in 栈一次
 * - 从 in 出栈一次
 * - 入 out 栈一次
 * - 从 out 出栈一次
 *
 * 总共 4 次操作，均摊到 n 次操作就是 O(1)。
 *
 * 倒腾时机的选择：
 * - 只在 out 栈空时才倒腾
 * - 不是每次出队都倒腾
 * - 这样避免了元素来回倒腾
 *
 * 类似的问题：
 * - 用队列实现栈：每次入队后把前面的元素重新入队
 * - 最小栈：用辅助栈记录每个状态的最小值」
 *
 * 🔄 可能的追问：
 * - 用队列实现栈怎么做？
 * - 能只用一个栈实现队列吗？（不能保证 O(1)）
 */

// ============================================================
// Q7: 逆波兰表达式是什么？怎么计算？
// ============================================================

/**
 * 📝 问题：什么是逆波兰表达式？如何用栈计算？
 *
 * ✅ 标准答案：
 * 逆波兰表达式（后缀表达式）是一种不需要括号的表达式表示法。
 *
 * 例如：3 + 4 × 2 → 3 4 2 × +
 *
 * 计算方法：
 * 1. 遍历表达式
 * 2. 遇到数字入栈
 * 3. 遇到运算符弹出两个数计算，结果入栈
 * 4. 最后栈中剩下的就是结果
 *
 * ⭐ 加分回答：
 * 「为什么用后缀表达式？
 *
 * 1. 不需要括号，没有优先级问题
 * 2. 用栈可以简单计算
 * 3. 编译器/解释器常用
 *
 * 计算过程示例：
 * 表达式 "3 4 2 × +"
 *
 * 3: 入栈 [3]
 * 4: 入栈 [3, 4]
 * 2: 入栈 [3, 4, 2]
 * ×: 弹出 2 和 4，计算 4×2=8，入栈 [3, 8]
 * +: 弹出 8 和 3，计算 3+8=11，入栈 [11]
 *
 * 结果：11
 *
 * 中缀转后缀：
 * - 用两个栈：操作数栈和操作符栈
 * - 考虑优先级和括号
 * - 是编译原理的基础知识
 *
 * 前端应用：
 * - 公式编辑器
 * - Excel 公式解析
 * - 自定义表达式引擎」
 *
 * 🔄 可能的追问：
 * - 如何将中缀表达式转换为后缀？
 * - 如何处理括号和优先级？
 */

// ============================================================
// Q8: 字符串解码怎么做？
// ============================================================

/**
 * 📝 问题：字符串解码 "3[a2[c]]" 怎么做？
 *
 * ✅ 标准答案：
 * 用两个栈：一个存数字，一个存字符串。
 *
 * 遍历字符串：
 * - 数字：累积（可能多位数）
 * - '['：当前数字和字符串入栈，重置
 * - ']'：出栈，拼接 str = prev_str + cur_str.repeat(num)
 * - 字母：累积到当前字符串
 *
 * ⭐ 加分回答：
 * 「处理嵌套的关键是"保存和恢复状态"：
 *
 * 示例 "3[a2[c]]"：
 *
 * 3: num=3
 * [: 入栈 (3, "")，重置 num=0, str=""
 * a: str="a"
 * 2: num=2
 * [: 入栈 (2, "a")，重置 num=0, str=""
 * c: str="c"
 * ]: 出栈 (2, "a")，str = "a" + "c".repeat(2) = "acc"
 * ]: 出栈 (3, "")，str = "" + "acc".repeat(3) = "accaccacc"
 *
 * 还可以用递归：
 * - 遇到 '[' 递归处理
 * - 遇到 ']' 返回结果
 * - 需要用引用传递索引
 *
 * 类似题目：
 * - 括号匹配（简单版）
 * - 原子的数量（更复杂）
 * - 表达式求值
 *
 * 共同模式：遇到 '[' 保存状态入栈，遇到 ']' 恢复状态出栈。」
 *
 * 🔄 可能的追问：
 * - 如何处理多位数？
 * - 递归解法怎么写？
 */

// ============================================================
// Q9: JavaScript 的调用栈了解吗？
// ============================================================

/**
 * 📝 问题：JavaScript 的调用栈是什么？和栈有什么关系？
 *
 * ✅ 标准答案：
 * 调用栈（Call Stack）是 JavaScript 引擎用于跟踪函数调用的栈结构。
 *
 * 特点：
 * - 每调用一个函数，就压入一个执行上下文
 * - 函数返回时，弹出执行上下文
 * - 栈溢出：递归太深会报 "Maximum call stack size exceeded"
 *
 * ⭐ 加分回答：
 * 「深入理解 JavaScript 调用栈：
 *
 * 1. 执行上下文包含：
 *    - 变量对象（变量、函数声明）
 *    - 作用域链
 *    - this 绑定
 *
 * 2. 栈溢出的原因：
 *    - 每次递归调用都占用栈空间
 *    - 栈空间有限（通常几 MB）
 *    - 尾调用优化可以避免，但 JS 支持有限
 *
 * 3. 与事件循环的关系：
 *    - 调用栈空了才会处理任务队列
 *    - 长时间占用调用栈会阻塞 UI
 *
 * 4. 调试时的应用：
 *    - Chrome DevTools 可以看调用栈
 *    - Error.stack 包含调用栈信息
 *
 * 如何避免栈溢出？
 * - 使用迭代代替递归
 * - 使用 setTimeout 拆分任务
 * - 使用蹦床函数（trampoline）
 *
 * 这就是为什么算法面试中经常问"递归转迭代"。」
 *
 * 🔄 可能的追问：
 * - 什么是尾调用优化？
 * - 如何把递归转成迭代？
 */

// ============================================================
// Q10: 最小栈怎么实现？
// ============================================================

/**
 * 📝 问题：如何实现一个支持 O(1) 获取最小值的栈？
 *
 * ✅ 标准答案：
 * 用辅助栈记录每个状态对应的最小值。
 *
 * 操作：
 * - push(x)：主栈入 x，辅助栈入 min(x, 当前最小)
 * - pop()：两个栈都 pop
 * - getMin()：辅助栈栈顶就是最小值
 *
 * 时间 O(1)，空间 O(n)。
 *
 * ⭐ 加分回答：
 * 「还有空间优化的方法：
 *
 * 方法一：辅助栈只在更小时入栈
 * - push 时只有 x <= 当前最小才入辅助栈
 * - pop 时只有弹出的是最小值才同步弹辅助栈
 * - 空间最优情况 O(1)，最差 O(n)
 *
 * 方法二：一个栈存差值
 * - 存 x - min 的差值
 * - 如果差值为负，说明 x 更小，更新 min
 * - 实现复杂，容易出错
 *
 * 方法三：一个栈存对象
 * - 每个元素是 {value, min} 对
 * - 简单直观
 *
 * 面试中推荐方法一（辅助栈优化版），
 * 代码简洁且能讲清楚优化思路。
 *
 * 类似题目：
 * - 最大栈（支持 popMax）
 * - 栈排序」
 *
 * 🔄 可能的追问：
 * - 如何实现支持 O(1) 删除任意元素的栈？
 * - 如何实现最大栈？
 */

// ============================================================
// 📋 面试问答速查表
// ============================================================

/**
 * ┌─────────────────────────────────────────────────────────────────────────┐
 * │                           面试问答速查表                                 │
 * ├────────────────────────────────┬────────────────────────────────────────┤
 * │           问题类型             │              核心答案要点               │
 * ├────────────────────────────────┼────────────────────────────────────────┤
 * │ 栈 vs 队列                     │ LIFO vs FIFO，信号词判断               │
 * │ 单调栈是什么                   │ 找下一个更大/小，找大用递减栈           │
 * │ 柱状图最大矩形                 │ 单调递增栈，找左右边界                 │
 * │ 接雨水解法                     │ DP/双指针/单调栈，双指针空间最优       │
 * │ 单调队列 vs 单调栈             │ 双端操作 vs 单端，滑动窗口最值         │
 * │ 两个栈实现队列                 │ in 栈入，out 栈出，均摊 O(1)           │
 * │ 逆波兰表达式                   │ 后缀表达式，数字入栈运算符弹出计算     │
 * │ 字符串解码                     │ 双栈保存恢复状态，[ 入栈 ] 出栈        │
 * │ JavaScript 调用栈              │ 执行上下文栈，栈溢出，事件循环         │
 * │ 最小栈                         │ 辅助栈同步记录最小值                   │
 * └────────────────────────────────┴────────────────────────────────────────┘
 */

// ============================================================
// 🎯 回答问题的技巧
// ============================================================

/**
 * 栈与队列题目的回答模板：
 *
 * 1. 识别数据结构：
 *    「这道题用[栈/队列/单调栈/单调队列]」
 *
 * 2. 说明核心思想：
 *    「栈用来[匹配/保存状态/单调性维护]」
 *
 * 3. 模拟过程：
 *    「遍历时，遇到 xxx 就入栈，遇到 xxx 就出栈...」
 *
 * 4. 复杂度分析：
 *    「时间 O(n)，因为每个元素入栈出栈各一次」
 *
 * 示例回答（每日温度）：
 * 「这道题是找下一个更大元素，用单调递减栈。
 * 遍历温度数组，如果当前温度比栈顶大，
 * 说明找到了栈顶的下一个更大温度，弹出栈顶并记录天数差。
 * 重复直到栈空或当前温度 <= 栈顶，然后当前索引入栈。
 * 时间 O(n)，因为每个元素入栈出栈各一次。」
 */

export {};

