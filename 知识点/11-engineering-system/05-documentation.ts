/**
 * ============================================================
 * 📚 文档与规范
 * ============================================================
 *
 * 面试考察重点：
 * 1. 技术文档体系
 * 2. API 文档
 * 3. 组件文档
 * 4. 团队规范
 */

// ============================================================
// 1. 文档体系
// ============================================================

/**
 * 📊 技术文档分类
 *
 * ┌─────────────────────────────────────────────────────────────────┐
 * │                      技术文档体系                               │
 * │                                                                 │
 * │  ┌─────────────────────────────────────────────────────────┐   │
 * │  │                    项目文档                              │   │
 * │  │  README │ 快速开始 │ 架构说明 │ 部署指南                  │   │
 * │  └─────────────────────────────────────────────────────────┘   │
 * │                                                                 │
 * │  ┌─────────────────────────────────────────────────────────┐   │
 * │  │                    API 文档                              │   │
 * │  │  接口定义 │ 参数说明 │ 示例代码 │ 错误码                  │   │
 * │  └─────────────────────────────────────────────────────────┘   │
 * │                                                                 │
 * │  ┌─────────────────────────────────────────────────────────┐   │
 * │  │                   组件文档                               │   │
 * │  │  Props │ 示例 │ 最佳实践 │ 更新日志                      │   │
 * │  └─────────────────────────────────────────────────────────┘   │
 * │                                                                 │
 * │  ┌─────────────────────────────────────────────────────────┐   │
 * │  │                   团队规范                               │   │
 * │  │  编码规范 │ Git 规范 │ 设计规范 │ 流程规范               │   │
 * │  └─────────────────────────────────────────────────────────┘   │
 * └─────────────────────────────────────────────────────────────────┘
 */

// ============================================================
// 2. README 模板
// ============================================================

const readmeTemplate = `
# 项目名称

> 一句话描述项目

[![Build Status](badge-url)]()
[![Coverage](badge-url)]()
[![npm version](badge-url)]()

## ✨ 特性

- 特性 1
- 特性 2
- 特性 3

## 📦 安装

\`\`\`bash
npm install package-name
# or
pnpm add package-name
\`\`\`

## 🔨 快速开始

\`\`\`typescript
import { Component } from 'package-name';

// 基础用法
<Component prop="value" />
\`\`\`

## 📚 API 文档

### Props

| 参数 | 说明 | 类型 | 默认值 |
| --- | --- | --- | --- |
| prop1 | 描述 | string | - |
| prop2 | 描述 | number | 0 |

### 方法

#### method(param)

描述方法功能

**参数**

- \`param\`: \`string\` - 参数描述

**返回值**

\`Promise<Result>\`

## 🔗 相关链接

- [在线演示](demo-url)
- [更新日志](changelog-url)
- [贡献指南](contributing-url)

## 📄 License

MIT
`;

// ============================================================
// 3. 组件文档（Storybook）
// ============================================================

/**
 * 📊 Storybook 组件文档
 *
 * - 交互式组件展示
 * - Props 文档自动生成
 * - 可视化测试
 */

const storybookExample = `
// Button.stories.tsx
import type { Meta, StoryObj } from '@storybook/react';
import { Button } from './Button';

const meta: Meta<typeof Button> = {
  title: 'Components/Button',
  component: Button,
  tags: ['autodocs'],
  argTypes: {
    variant: {
      control: 'select',
      options: ['primary', 'secondary', 'danger'],
      description: '按钮类型',
    },
    size: {
      control: 'radio',
      options: ['small', 'medium', 'large'],
      description: '按钮大小',
    },
    disabled: {
      control: 'boolean',
      description: '是否禁用',
    },
    onClick: { action: 'clicked' },
  },
  parameters: {
    docs: {
      description: {
        component: '基础按钮组件，支持多种样式和状态',
      },
    },
  },
};

export default meta;
type Story = StoryObj<typeof Button>;

// 基础用法
export const Primary: Story = {
  args: {
    variant: 'primary',
    children: '主要按钮',
  },
};

// 次要按钮
export const Secondary: Story = {
  args: {
    variant: 'secondary',
    children: '次要按钮',
  },
};

// 危险按钮
export const Danger: Story = {
  args: {
    variant: 'danger',
    children: '危险操作',
  },
};

// 禁用状态
export const Disabled: Story = {
  args: {
    disabled: true,
    children: '禁用按钮',
  },
};

// 不同大小
export const Sizes: Story = {
  render: () => (
    <div style={{ display: 'flex', gap: 8, alignItems: 'center' }}>
      <Button size="small">Small</Button>
      <Button size="medium">Medium</Button>
      <Button size="large">Large</Button>
    </div>
  ),
};
`;

// ============================================================
// 4. API 文档
// ============================================================

/**
 * 📊 API 文档工具
 *
 * - OpenAPI/Swagger
 * - TypeDoc
 * - JSDoc
 */

const typedocExample = `
// tsconfig.json
{
  "compilerOptions": {
    "declaration": true,
    "declarationDir": "./types"
  }
}

// typedoc.json
{
  "entryPoints": ["./src/index.ts"],
  "out": "./docs",
  "name": "My Library",
  "includeVersion": true,
  "excludePrivate": true,
  "excludeInternal": true,
  "readme": "README.md"
}

// 代码注释示例
/**
 * 用户服务类
 * 
 * @example
 * \`\`\`typescript
 * const userService = new UserService();
 * const user = await userService.getUser('123');
 * \`\`\`
 * 
 * @public
 */
export class UserService {
  /**
   * 获取用户信息
   * 
   * @param id - 用户 ID
   * @returns 用户信息
   * @throws {NotFoundError} 用户不存在时抛出
   * 
   * @example
   * \`\`\`typescript
   * const user = await userService.getUser('123');
   * console.log(user.name);
   * \`\`\`
   */
  async getUser(id: string): Promise<User> {
    // ...
  }

  /**
   * 创建用户
   * 
   * @param data - 用户数据
   * @param data.name - 用户名
   * @param data.email - 邮箱地址
   * @returns 创建的用户
   */
  async createUser(data: CreateUserDTO): Promise<User> {
    // ...
  }
}
`;

// ============================================================
// 5. 团队规范文档
// ============================================================

const teamStandardsTemplate = `
# 前端开发规范

## 1. 项目结构

\`\`\`
src/
├── assets/          # 静态资源
├── components/      # 公共组件
│   └── Button/
│       ├── index.tsx
│       ├── Button.tsx
│       ├── Button.test.tsx
│       ├── Button.stories.tsx
│       └── Button.module.css
├── hooks/           # 自定义 Hooks
├── pages/           # 页面组件
├── services/        # API 服务
├── stores/          # 状态管理
├── utils/           # 工具函数
└── types/           # 类型定义
\`\`\`

## 2. 命名规范

### 2.1 文件命名

- 组件文件：PascalCase（Button.tsx）
- 工具函数：camelCase（formatDate.ts）
- 常量文件：camelCase（constants.ts）
- 类型文件：camelCase（types.ts）
- 测试文件：*.test.ts 或 *.spec.ts

### 2.2 变量命名

- 变量/函数：camelCase
- 常量：UPPER_SNAKE_CASE
- 类/组件：PascalCase
- 私有属性：_camelCase
- 布尔值：is/has/can 前缀

## 3. Git 规范

### 3.1 分支命名

- 功能分支：feature/功能名
- 修复分支：fix/问题描述
- 热修复：hotfix/问题描述
- 发布分支：release/版本号

### 3.2 提交信息

\`\`\`
<type>(<scope>): <subject>

<body>

<footer>
\`\`\`

type: feat | fix | docs | style | refactor | perf | test | chore

## 4. 代码审查清单

- [ ] 代码是否符合规范
- [ ] 是否有足够的测试
- [ ] 是否有性能问题
- [ ] 是否有安全隐患
- [ ] 文档是否更新

## 5. 发布流程

1. 创建 release 分支
2. 更新版本号和 CHANGELOG
3. 提交 PR 到 main
4. Code Review 通过
5. 合并并打 Tag
6. CI/CD 自动部署
`;

// ============================================================
// 6. CHANGELOG 自动生成
// ============================================================

const changelogExample = `
// 使用 standard-version 自动生成 CHANGELOG

// package.json
{
  "scripts": {
    "release": "standard-version",
    "release:minor": "standard-version --release-as minor",
    "release:major": "standard-version --release-as major"
  }
}

// .versionrc
{
  "types": [
    { "type": "feat", "section": "✨ Features" },
    { "type": "fix", "section": "🐛 Bug Fixes" },
    { "type": "perf", "section": "⚡ Performance" },
    { "type": "refactor", "section": "♻️ Refactor" },
    { "type": "docs", "section": "📝 Documentation" },
    { "type": "chore", "hidden": true }
  ]
}

// 生成的 CHANGELOG.md 示例
# Changelog

## [1.2.0](https://github.com/xxx/compare/v1.1.0...v1.2.0) (2024-01-15)

### ✨ Features

* **button:** 新增 loading 状态 ([#123](https://github.com/xxx/issues/123))
* **form:** 支持自定义校验规则

### 🐛 Bug Fixes

* **input:** 修复中文输入问题 ([#124](https://github.com/xxx/issues/124))

### ⚡ Performance

* **table:** 优化大数据渲染性能
`;

// ============================================================
// 7. ⚠️ 注意事项（易错点）
// ============================================================

/**
 * ⚠️ 常见问题
 *
 * 1. 文档不及时更新
 *    - 代码变更同步更新文档
 *    - 文档作为 PR 检查项
 *
 * 2. 文档不可执行
 *    - 示例代码要可运行
 *    - 定期验证文档
 *
 * 3. 规范太多太细
 *    - 精简核心规范
 *    - 工具自动化
 *
 * 4. 规范不落地
 *    - 配合工具强制执行
 *    - 定期 Review
 */

// ============================================================
// 8. 💡 面试追问
// ============================================================

/**
 * 💡 深度追问
 *
 * Q1: 如何保证文档的及时性？
 * A:
 *    - 文档作为 PR 必需项
 *    - 自动化生成（TypeDoc）
 *    - 定期 Review
 *
 * Q2: 如何让规范落地？
 * A:
 *    - ESLint/Prettier 自动检查
 *    - Git hooks 强制执行
 *    - CI 质量门禁
 *    - Code Review
 *
 * Q3: 组件文档用什么工具？
 * A:
 *    - Storybook：交互式组件文档
 *    - Docusaurus：静态文档站点
 *    - VitePress：Vue 生态
 *
 * Q4: 如何管理 API 文档？
 * A:
 *    - OpenAPI/Swagger 定义
 *    - 代码注释生成
 *    - Mock 服务联动
 */

export {
  readmeTemplate,
  storybookExample,
  typedocExample,
  teamStandardsTemplate,
  changelogExample,
};

