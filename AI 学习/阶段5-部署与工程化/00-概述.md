# 🚀 阶段5：部署与 MLOps / LLMOps

> 将 LLM 应用从开发环境推向生产

---

## 阶段目标

```
完成本阶段后，你将能够：
✅ 理解本地/私有化部署 LLM 的常见方案和权衡
✅ 使用推理引擎（Ollama / vLLM）搭建高吞吐服务
✅ 用 FastAPI + Docker 封装和部署 LLM/RAG 接口
✅ 理解基本的监控、日志记录、RAG 评估方法
✅ 掌握 Prompt 注入防护等安全最佳实践
```

---

## 学习计划（2-3 周）

| 周次 | 主题 | 内容 |
|------|------|------|
| Week 1 | 推理基础 | 量化格式、推理优化、Ollama、vLLM |
| Week 2 | 服务化 | FastAPI、流式输出、认证限流、Docker |
| Week 3 | 运维监控 | 日志、评估、安全、完整项目 |

---

## 环境准备

### 基础依赖

```bash
# 创建环境
conda create -n llm-deploy python=3.11 -y
conda activate llm-deploy

# 核心库
pip install fastapi uvicorn python-multipart
pip install openai httpx sse-starlette
pip install pydantic pydantic-settings
pip install python-jose[cryptography] passlib[bcrypt]

# 监控日志
pip install structlog prometheus-client
pip install langsmith langfuse

# RAG 评估
pip install ragas datasets

# Docker（系统安装）
# macOS: brew install --cask docker
# Linux: curl -fsSL https://get.docker.com | sh
```

### 推理引擎安装

```bash
# Ollama（最简单）
curl -fsSL https://ollama.com/install.sh | sh

# vLLM（高吞吐，需要 GPU）
pip install vllm

# llama.cpp（CPU 友好）
# 从 GitHub releases 下载预编译版本
```

### GPU 环境（可选）

```bash
# NVIDIA Container Toolkit
distribution=$(. /etc/os-release;echo $ID$VERSION_ID)
curl -s -L https://nvidia.github.io/nvidia-docker/gpgkey | sudo apt-key add -
curl -s -L https://nvidia.github.io/nvidia-docker/$distribution/nvidia-docker.list | \
  sudo tee /etc/apt/sources.list.d/nvidia-docker.list
sudo apt-get update
sudo apt-get install -y nvidia-container-toolkit
sudo systemctl restart docker

# 验证
docker run --rm --gpus all nvidia/cuda:12.0-base nvidia-smi
```

---

## 内容模块

```
本阶段包含以下模块：

1. 部署与推理基础
   ├── 本地 vs 云端选择
   ├── 模型量化（FP16/INT8/INT4）
   ├── 量化格式（GGUF/GPTQ/AWQ）
   └── 推理优化技术

2. 推理引擎
   ├── Ollama（入门首选）
   ├── vLLM（生产首选）
   └── OpenAI 兼容接口

3. 服务化
   ├── FastAPI 封装
   ├── 流式输出（SSE）
   ├── 认证与限流
   └── 并发控制

4. 容器化
   ├── Dockerfile 编写
   ├── GPU 容器支持
   ├── Docker Compose
   └── CI/CD 基础

5. 监控与安全
   ├── 日志记录
   ├── RAG 评估（Ragas）
   ├── 调用链追踪（LangSmith）
   └── 安全防护
```

---

## 技术架构预览

```
┌─────────────────────────────────────────────────────────────┐
│                     生产级 LLM 服务架构                       │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   ┌──────────┐     ┌──────────┐     ┌──────────┐          │
│   │  Nginx   │────→│ FastAPI  │────→│  vLLM    │          │
│   │ (反向代理)│     │ (API)    │     │ (推理)   │          │
│   └──────────┘     └──────────┘     └──────────┘          │
│        │                │                                   │
│        ↓                ↓                                   │
│   ┌──────────┐     ┌──────────┐     ┌──────────┐          │
│   │ 限流/认证 │     │   日志   │     │  向量DB  │          │
│   └──────────┘     └──────────┘     └──────────┘          │
│                                                             │
│   ┌─────────────────────────────────────────────┐          │
│   │              监控 (Prometheus + Grafana)      │          │
│   └─────────────────────────────────────────────┘          │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 学习资源

```
官方文档：
- Ollama: https://ollama.com/
- vLLM: https://docs.vllm.ai/
- FastAPI: https://fastapi.tiangolo.com/
- Docker: https://docs.docker.com/

工具文档：
- Ragas: https://docs.ragas.io/
- LangSmith: https://docs.smith.langchain.com/
- LangFuse: https://langfuse.com/docs
```

---

## ➡️ 开始学习

继续 [01-部署基础.md](./01-部署基础.md)

