# ✅ 11 - 阶段 0 自测清单

> 学完阶段 0 后，检验你的学习成果

---

## 📋 知识点自测

### 全局认知

- [ ] 1. 简述 AI 技术的演进路线：传统 ML → 深度学习 → 大模型
- [ ] 2. 监督学习和无监督学习的区别是什么？
- [ ] 3. 什么是自监督学习？为什么它对大模型很重要？
- [ ] 4. AI 工程师有哪些主要方向？各自需要什么技能？

### Python 基础

- [ ] 5. 列表和元组的区别是什么？
- [ ] 6. 什么是列表推导式？写一个例子
- [ ] 7. 装饰器的作用是什么？
- [ ] 8. 生成器和普通函数的区别是什么？
- [ ] 9. 什么是类型注解？它有什么好处？

### 数据科学三剑客

- [ ] 10. NumPy 数组相比 Python 列表有什么优势？
- [ ] 11. 什么是广播机制？
- [ ] 12. Pandas 中 DataFrame 和 Series 的区别是什么？
- [ ] 13. 如何处理 DataFrame 中的缺失值？
- [ ] 14. groupby 的作用是什么？

### 工具与环境

- [ ] 15. 为什么需要虚拟环境？
- [ ] 16. Conda 和 pip 的区别是什么？
- [ ] 17. Git 的基本工作流程是什么？

### 数学直觉

- [ ] 18. 向量点积的几何意义是什么？
- [ ] 19. 梯度下降的核心思想是什么？
- [ ] 20. Softmax 函数的作用是什么？

---

## ✅ 参考答案

<details>
<summary>点击查看答案</summary>

### 1. AI 技术演进
- **传统 ML**：特征工程 + 简单模型（决策树、SVM）
- **深度学习**：端到端学习，自动提取特征（CNN、RNN）
- **大模型**：大规模预训练 + 微调/提示，涌现能力（GPT、LLaMA）

### 2. 监督 vs 无监督
- **监督学习**：有标签数据，学习输入到输出的映射
- **无监督学习**：无标签数据，发现数据中的结构

### 3. 自监督学习
从数据本身构造监督信号（如预测下一个词）。对大模型重要是因为可以利用海量无标签数据进行预训练。

### 4. AI 工程师方向
- **算法研究**：数学、论文、模型创新
- **应用开发**：Python、API、产品落地
- **模型训练**：PyTorch、分布式、GPU
- **部署运维**：Docker、K8s、推理优化

### 5. 列表 vs 元组
- **列表**：可变，用 `[]`
- **元组**：不可变，用 `()`

### 6. 列表推导式
```python
squares = [x**2 for x in range(10)]
```

### 7. 装饰器
在不修改函数代码的情况下，给函数添加额外功能（如日志、计时）

### 8. 生成器 vs 函数
- **函数**：一次返回所有结果
- **生成器**：惰性计算，每次 yield 一个值，节省内存

### 9. 类型注解
```python
def add(a: int, b: int) -> int:
    return a + b
```
好处：代码可读性、IDE 提示、静态检查

### 10. NumPy 优势
- 内存连续，访问更快
- 底层用 C 实现，计算更快
- 支持向量化运算

### 11. 广播机制
不同形状的数组可以进行运算，较小的数组会自动扩展：
```python
arr + 10  # 标量扩展为与 arr 相同形状
```

### 12. DataFrame vs Series
- **Series**：一维带标签数组
- **DataFrame**：二维表格，由多个 Series 组成

### 13. 处理缺失值
```python
df.dropna()      # 删除
df.fillna(0)     # 填充
df.fillna(df.mean())  # 用均值填充
```

### 14. groupby
按某列分组，然后对每组进行聚合计算：
```python
df.groupby('category')['sales'].sum()
```

### 15. 为什么需要虚拟环境
不同项目需要不同版本的包，虚拟环境可以隔离依赖，避免冲突

### 16. Conda vs pip
- **Conda**：环境管理 + 包管理，可管理非 Python 包
- **pip**：纯 Python 包管理

### 17. Git 基本流程
```bash
git add .           # 暂存
git commit -m "..."  # 提交
git push            # 推送
git pull            # 拉取
```

### 18. 点积的几何意义
- 两个向量的相似程度
- `a · b = |a||b|cos(θ)`
- 相同方向：最大；垂直：0；相反：负

### 19. 梯度下降
沿着梯度的反方向（函数下降最快的方向）更新参数：
```
参数 = 参数 - 学习率 × 梯度
```

### 20. Softmax
把任意实数向量转换成概率分布（所有值在 0-1 之间，和为 1）

</details>

---

## 💻 代码能力自测

### 1. Python 基础

写一个函数，接收一个列表，返回去重后按降序排列的结果。

<details>
<summary>参考答案</summary>

```python
def unique_sorted_desc(lst):
    return sorted(set(lst), reverse=True)

# 测试
print(unique_sorted_desc([3, 1, 4, 1, 5, 9, 2, 6, 5]))
# [9, 6, 5, 4, 3, 2, 1]
```

</details>

### 2. NumPy

用 NumPy 计算两个向量的余弦相似度。

<details>
<summary>参考答案</summary>

```python
import numpy as np

def cosine_similarity(a, b):
    return np.dot(a, b) / (np.linalg.norm(a) * np.linalg.norm(b))

# 测试
v1 = np.array([1, 2, 3])
v2 = np.array([4, 5, 6])
print(f"余弦相似度: {cosine_similarity(v1, v2):.4f}")
# 0.9746
```

</details>

### 3. Pandas

给定一个 DataFrame，按 `category` 分组，计算每组的 `sales` 总和和平均值。

<details>
<summary>参考答案</summary>

```python
import pandas as pd

df = pd.DataFrame({
    'category': ['A', 'A', 'B', 'B', 'C'],
    'sales': [100, 200, 150, 250, 300]
})

result = df.groupby('category')['sales'].agg(['sum', 'mean'])
print(result)
#          sum   mean
# category
# A        300  150.0
# B        400  200.0
# C        300  300.0
```

</details>

### 4. Matplotlib

画一个折线图，显示 y = sin(x) 在 [0, 2π] 区间的曲线。

<details>
<summary>参考答案</summary>

```python
import numpy as np
import matplotlib.pyplot as plt

x = np.linspace(0, 2 * np.pi, 100)
y = np.sin(x)

plt.figure(figsize=(10, 6))
plt.plot(x, y, 'b-', linewidth=2)
plt.title('y = sin(x)')
plt.xlabel('x')
plt.ylabel('y')
plt.grid(True, alpha=0.3)
plt.show()
```

</details>

### 5. 综合应用

实现一个简单的梯度下降，找到 f(x) = x² - 4x + 4 的最小值点。

<details>
<summary>参考答案</summary>

```python
import numpy as np

def f(x):
    return x**2 - 4*x + 4

def gradient(x):
    return 2*x - 4

def gradient_descent(start, lr, iterations):
    x = start
    history = [x]

    for _ in range(iterations):
        grad = gradient(x)
        x = x - lr * grad
        history.append(x)

    return x, history

# 运行
x_min, history = gradient_descent(start=10, lr=0.1, iterations=50)
print(f"最小值点: x = {x_min:.4f}")
print(f"f(x) = {f(x_min):.4f}")
# x ≈ 2, f(x) ≈ 0
```

</details>

---

## 🎯 下一步行动

完成自测后：

1. **分数 < 60%**：重新学习未掌握的章节
2. **分数 60-80%**：针对性复习薄弱点
3. **分数 > 80%**：进入 [阶段 1：传统机器学习](../阶段1-传统机器学习/00-概述.md)

---

## 📚 推荐巩固资源

### Python 练习
- [LeetCode Python 练习](https://leetcode.cn/)
- [Python 练习题](https://www.w3resource.com/python-exercises/)

### 数据分析项目
- [Kaggle 数据集](https://www.kaggle.com/datasets)
- [UCI Machine Learning Repository](https://archive.ics.uci.edu/ml/index.php)

### 数学复习
- [3Blue1Brown 线性代数](https://www.bilibili.com/video/BV1ys411472E)
- [3Blue1Brown 微积分](https://www.bilibili.com/video/BV1qW411N7FU)

> 💪 **记住：基础扎实，后面学习会更轻松！**

