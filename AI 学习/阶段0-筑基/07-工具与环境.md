# 🛠️ 07 - 工具与环境

> 搭建高效的 AI 开发环境

---

## 目录

1. [Python 环境管理](#1-python-环境管理)
2. [包管理工具](#2-包管理工具)
3. [Jupyter Notebook](#3-jupyter-notebook)
4. [VS Code 配置](#4-vs-code-配置)
5. [Git 基础](#5-git-基础)
6. [云端环境](#6-云端环境)

---

## 1. Python 环境管理

### 1.1 为什么需要虚拟环境？

```
问题：
- 项目 A 需要 numpy 1.20
- 项目 B 需要 numpy 1.24
- 全局安装会冲突！

解决：每个项目一个独立的虚拟环境
```

### 1.2 Conda（推荐）

```bash
# 安装 Miniconda（轻量版）
# 下载地址：https://docs.conda.io/en/latest/miniconda.html

# 创建环境
conda create -n my_env python=3.11

# 激活环境
conda activate my_env

# 退出环境
conda deactivate

# 查看所有环境
conda env list

# 删除环境
conda remove -n my_env --all

# 导出环境
conda env export > environment.yml

# 从文件创建环境
conda env create -f environment.yml

# 安装包
conda install numpy pandas matplotlib

# 查看已安装的包
conda list
```

### 1.3 venv（Python 内置）

```bash
# 创建虚拟环境
python -m venv my_venv

# 激活（Linux/Mac）
source my_venv/bin/activate

# 激活（Windows）
my_venv\Scripts\activate

# 退出
deactivate

# 删除（直接删除文件夹）
rm -rf my_venv
```

### 1.4 环境选择建议

| 工具 | 优点 | 缺点 | 适用场景 |
|------|------|------|---------|
| **Conda** | 管理多版本 Python、科学计算包优化 | 体积大 | AI/ML 开发 |
| **venv** | 轻量、Python 内置 | 功能简单 | 普通 Python 项目 |

---

## 2. 包管理工具

### 2.1 pip

```bash
# 安装包
pip install numpy
pip install numpy==1.24.0  # 指定版本
pip install numpy>=1.20    # 最低版本

# 升级包
pip install --upgrade numpy

# 卸载包
pip uninstall numpy

# 查看已安装
pip list
pip show numpy  # 查看详情

# 导出依赖
pip freeze > requirements.txt

# 从文件安装
pip install -r requirements.txt

# 使用国内镜像加速
pip install numpy -i https://pypi.tuna.tsinghua.edu.cn/simple

# 永久配置镜像
pip config set global.index-url https://pypi.tuna.tsinghua.edu.cn/simple
```

### 2.2 uv（新一代工具，更快）

```bash
# 安装 uv
pip install uv

# 使用 uv（语法类似 pip）
uv pip install numpy
uv pip install -r requirements.txt

# uv 比 pip 快 10-100 倍！
```

### 2.3 requirements.txt 最佳实践

```txt
# requirements.txt
numpy>=1.20.0,<2.0.0
pandas>=1.5.0
matplotlib>=3.5.0
scikit-learn>=1.0.0
torch>=2.0.0
transformers>=4.30.0

# 开发依赖
jupyter>=1.0.0
pytest>=7.0.0
black>=23.0.0
```

---

## 3. Jupyter Notebook

### 3.1 安装和启动

```bash
# 安装
pip install jupyter jupyterlab

# 启动 Jupyter Notebook
jupyter notebook

# 启动 JupyterLab（推荐，更现代）
jupyter lab

# 指定端口
jupyter lab --port 8889
```

### 3.2 基本操作

```
快捷键（命令模式，按 Esc 进入）：
- Enter: 进入编辑模式
- A: 上方插入单元格
- B: 下方插入单元格
- DD: 删除单元格
- M: 转为 Markdown
- Y: 转为代码
- Shift+Enter: 运行并跳到下一个
- Ctrl+Enter: 运行当前单元格

编辑模式（按 Enter 进入）：
- Tab: 代码补全
- Shift+Tab: 查看文档
- Ctrl+/: 注释/取消注释
```

### 3.3 魔法命令

```python
# 计时
%time sum(range(1000000))          # 单次计时
%timeit sum(range(1000000))        # 多次计时取平均

# 运行脚本
%run my_script.py

# 查看变量
%who      # 列出变量
%whos     # 详细信息

# 系统命令
!pip install numpy
!ls -la

# 显示图表（Jupyter 中）
%matplotlib inline

# 自动重载模块
%load_ext autoreload
%autoreload 2

# 查看帮助
?print           # 简短帮助
??print          # 详细帮助（源码）
```

### 3.4 Markdown 单元格

```markdown
# 标题 1
## 标题 2

**加粗** 和 *斜体*

- 列表项 1
- 列表项 2

1. 有序列表
2. 第二项

```python
# 代码块
print("Hello")
```

数学公式（LaTeX）：
$E = mc^2$

$$
\frac{\partial f}{\partial x} = 2x
$$

| 列1 | 列2 |
|-----|-----|
| A   | B   |
```

---

## 4. VS Code 配置

### 4.1 必装插件

```
Python 开发：
- Python（Microsoft）
- Pylance（智能提示）
- Jupyter（Notebook 支持）

AI/ML 相关：
- autoDocstring（自动生成文档字符串）
- Python Indent（智能缩进）
- Error Lens（行内显示错误）

通用工具：
- GitLens（Git 增强）
- Material Icon Theme（文件图标）
- One Dark Pro（主题）
```

### 4.2 Python 配置

```json
// settings.json
{
    "python.defaultInterpreterPath": "/path/to/python",
    "python.formatting.provider": "black",
    "python.linting.enabled": true,
    "python.linting.pylintEnabled": true,
    "editor.formatOnSave": true,
    "editor.codeActionsOnSave": {
        "source.organizeImports": true
    }
}
```

### 4.3 常用快捷键

```
编辑：
- Ctrl+D: 选中下一个相同词
- Ctrl+Shift+L: 选中所有相同词
- Alt+Up/Down: 移动行
- Ctrl+/: 注释/取消注释
- Ctrl+Shift+K: 删除行

导航：
- Ctrl+P: 快速打开文件
- Ctrl+Shift+P: 命令面板
- Ctrl+G: 跳转到行
- F12: 跳转到定义
- Alt+F12: 查看定义

调试：
- F5: 开始调试
- F9: 设置断点
- F10: 单步跳过
- F11: 单步进入

终端：
- Ctrl+`: 打开终端
- Ctrl+Shift+`: 新建终端
```

---

## 5. Git 基础

### 5.1 配置

```bash
# 设置用户信息
git config --global user.name "Your Name"
git config --global user.email "your@email.com"

# 查看配置
git config --list
```

### 5.2 基本操作

```bash
# 初始化仓库
git init

# 克隆仓库
git clone https://github.com/user/repo.git

# 查看状态
git status

# 添加到暂存区
git add file.py          # 单个文件
git add .                # 所有文件

# 提交
git commit -m "描述信息"

# 查看历史
git log
git log --oneline

# 推送到远程
git push origin main

# 拉取更新
git pull origin main
```

### 5.3 分支操作

```bash
# 查看分支
git branch

# 创建分支
git branch feature-x

# 切换分支
git checkout feature-x
# 或
git switch feature-x

# 创建并切换
git checkout -b feature-x
# 或
git switch -c feature-x

# 合并分支
git checkout main
git merge feature-x

# 删除分支
git branch -d feature-x
```

### 5.4 常见工作流

```bash
# 1. 开始新功能
git checkout -b feature/new-model

# 2. 工作中...
git add .
git commit -m "Add new model"

# 3. 推送分支
git push origin feature/new-model

# 4. 创建 Pull Request（在 GitHub 上）

# 5. 合并后删除分支
git checkout main
git pull
git branch -d feature/new-model
```

### 5.5 .gitignore

```gitignore
# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.eggs/
venv/
.env

# Jupyter
.ipynb_checkpoints/

# IDE
.vscode/
.idea/

# 数据和模型
data/
models/
*.csv
*.h5
*.pkl
*.pt
*.pth

# 日志
logs/
*.log

# 系统文件
.DS_Store
Thumbs.db
```

---

## 6. 云端环境

### 6.1 Google Colab

```python
# 访问：https://colab.research.google.com/

# 特点：
# - 免费 GPU/TPU
# - 无需配置环境
# - 与 Google Drive 集成

# 挂载 Google Drive
from google.colab import drive
drive.mount('/content/drive')

# 查看 GPU
!nvidia-smi

# 安装包
!pip install transformers

# 上传文件
from google.colab import files
uploaded = files.upload()

# 下载文件
files.download('model.pth')
```

### 6.2 Kaggle Notebooks

```python
# 访问：https://www.kaggle.com/code

# 特点：
# - 免费 GPU（每周 30 小时）
# - 直接访问 Kaggle 数据集
# - 可参加比赛

# 添加数据集：点击 "+ Add data"
# 数据路径：/kaggle/input/dataset-name/

# 保存输出
# 保存到 /kaggle/working/
```

### 6.3 云端 vs 本地

| 环境 | 优点 | 缺点 | 适用场景 |
|------|------|------|---------|
| **本地** | 完全控制、无网络依赖 | 需要 GPU、配置复杂 | 正式开发 |
| **Colab** | 免费 GPU、简单 | 会话断开、存储有限 | 学习、实验 |
| **Kaggle** | 免费 GPU、数据集多 | 时间限制 | 比赛、学习 |

---

## 练习

1. 创建一个 Conda 虚拟环境，安装 numpy、pandas、matplotlib
2. 在 Jupyter Notebook 中运行一段代码，使用魔法命令计时
3. 创建一个 Git 仓库，完成一次提交和推送
4. 在 Google Colab 中运行代码，查看 GPU 信息

---

## ➡️ 下一步

学完本节后，继续学习 [08-数学直觉.md](./08-数学直觉.md)

