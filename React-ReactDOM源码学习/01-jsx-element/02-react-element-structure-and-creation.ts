/**
 * ============================================================
 * 📚 Phase 1: JSX 与 React 元素 - Part 2: ReactElement 结构与创建
 * ============================================================
 *
 * 📁 核心源码位置:
 * - packages/react/src/ReactElement.js
 * - packages/react/src/jsx/ReactJSXElement.js
 * - packages/shared/ReactSymbols.js
 *
 * ⏱️ 预计时间：3-4 小时
 * 🎯 面试权重：⭐⭐⭐⭐⭐
 */

// ============================================================
// Part 1: ReactElement 的完整结构
// ============================================================

/**
 * 📊 ReactElement 字段详解
 */

const reactElementStructure = `
📊 ReactElement 的完整结构

📁 源码位置: packages/react/src/ReactElement.js (Line 148-202)

┌─────────────────────────────────────────────────────────────────────────────┐
│                    ReactElement 对象结构                                     │
├─────────────────────┬───────────────────────────────────────────────────────┤
│ 字段                │ 含义                                                   │
├─────────────────────┼───────────────────────────────────────────────────────┤
│ $$typeof            │ Symbol.for('react.element')，用于识别和安全校验       │
│ type                │ 元素类型（'div'、函数组件、类组件等）                 │
│ key                 │ 列表渲染的唯一标识，用于 Diff 算法                    │
│ ref                 │ 引用，用于获取 DOM 节点或组件实例                     │
│ props               │ 属性对象，包含 children 和其他属性                    │
│ _owner              │ 创建此元素的组件（Fiber），用于错误追踪               │
├─────────────────────┼───────────────────────────────────────────────────────┤
│ (DEV only)          │                                                       │
├─────────────────────┼───────────────────────────────────────────────────────┤
│ _store              │ 开发环境验证信息存储                                   │
│ _self               │ 开发环境 this 指向检测                                │
│ _source             │ 开发环境源码位置信息（文件名、行号）                   │
└─────────────────────┴───────────────────────────────────────────────────────┘

实际源码 (简化版):
─────────────────────────

const ReactElement = function(type, key, ref, self, source, owner, props) {
  const element = {
    // ⭐ 这个标记让我们能唯一识别这是一个 React Element
    $$typeof: REACT_ELEMENT_TYPE,

    // ⭐ 元素的内置属性
    type: type,
    key: key,
    ref: ref,
    props: props,

    // ⭐ 记录创建此元素的组件（用于错误追踪和 ref 解析）
    _owner: owner,
  };

  if (__DEV__) {
    element._store = {};
    // ... 开发环境的额外属性
  }

  return element;
};
`;

// ============================================================
// Part 2: 各字段详细说明
// ============================================================

/**
 * 📊 $$typeof 字段
 */

const typeofFieldExplanation = `
📊 $$typeof 字段详解

📁 定义位置: packages/shared/ReactSymbols.js

┌─────────────────────────────────────────────────────────────────────────────┐
│                       $$typeof 的作用                                        │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  export const REACT_ELEMENT_TYPE = Symbol.for('react.element');             │
│                                                                             │
│  1. 唯一标识                                                                │
│     ─────────────                                                           │
│     • 区分 ReactElement 和普通 JS 对象                                      │
│     • isValidElement() 函数通过检查此字段判断是否为合法元素                 │
│                                                                             │
│  2. 安全性（防 XSS 攻击）⭐⭐⭐                                              │
│     ─────────────                                                           │
│     • JSON 无法序列化 Symbol                                                │
│     • 防止攻击者通过注入 JSON 数据伪造 ReactElement                         │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘

安全性场景解释:
═══════════════════════════════════════════════════════════════════════════════

假设你的代码从服务器获取数据并渲染：

// 服务器返回的数据
const serverData = await fetch('/api/message').then(r => r.json());

// 如果攻击者可以控制服务器响应，返回：
{
  "type": "div",
  "props": {
    "dangerouslySetInnerHTML": {
      "__html": "<script>恶意代码</script>"
    }
  }
}

// 如果没有 $$typeof 检查，这个对象可能被当作 ReactElement 渲染！

// 但是，$$typeof 使用 Symbol，而 JSON.parse 无法创建 Symbol
// 所以攻击者无法伪造有效的 ReactElement：

JSON.stringify({ $$typeof: Symbol.for('react.element') })
// 结果: "{}"  ← Symbol 被忽略了！

// React 渲染时会检查：
function isValidElement(object) {
  return (
    typeof object === 'object' &&
    object !== null &&
    object.$$typeof === REACT_ELEMENT_TYPE  // ⭐ Symbol 比较
  );
}
`;

/**
 * 📊 type 字段
 */

const typeFieldExplanation = `
📊 type 字段详解

┌─────────────────────────────────────────────────────────────────────────────┐
│                       type 的可能值                                          │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  type 字段标识元素的"类型"，决定了 React 如何处理这个元素                   │
│                                                                             │
│  1. 原生 DOM 元素 → 字符串                                                  │
│     ─────────────                                                           │
│     <div>    → type: 'div'                                                  │
│     <span>   → type: 'span'                                                 │
│     <input>  → type: 'input'                                                │
│                                                                             │
│  2. 函数组件 → 函数引用                                                     │
│     ─────────────                                                           │
│     function Hello() { return <div>Hi</div>; }                              │
│     <Hello /> → type: Hello (函数本身)                                      │
│                                                                             │
│  3. 类组件 → 类引用                                                         │
│     ─────────────                                                           │
│     class App extends React.Component { ... }                               │
│     <App /> → type: App (类本身)                                            │
│                                                                             │
│  4. Fragment → Symbol                                                       │
│     ─────────────                                                           │
│     <> ... </> → type: Symbol.for('react.fragment')                         │
│                                                                             │
│  5. 其他内置类型 → Symbol                                                   │
│     ─────────────                                                           │
│     <React.Suspense> → type: Symbol.for('react.suspense')                   │
│     <React.StrictMode> → type: Symbol.for('react.strict_mode')              │
│     <SomeContext.Provider> → type: { $$typeof: REACT_PROVIDER_TYPE, ... }   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘

在后续阶段的使用:
─────────────────────────

Phase 2 (Fiber 架构):
  - 根据 type 决定创建什么类型的 Fiber 节点
  - 字符串 → HostComponent Fiber
  - 函数 → FunctionComponent Fiber
  - 类 → ClassComponent Fiber

Phase 5 (Diff 算法):
  - 比较 type 是否相同决定是否可以复用节点
  - type 不同 → 销毁旧节点，创建新节点
`;

/**
 * 📊 key 和 ref 字段
 */

const keyAndRefExplanation = `
📊 key 和 ref 字段详解

┌─────────────────────────────────────────────────────────────────────────────┐
│                       key 字段                                               │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  作用: 帮助 React 识别列表中哪些元素改变、添加或删除                        │
│                                                                             │
│  // 没有 key                                                                │
│  items.map(item => <Item data={item} />)                                    │
│  // key 默认为 index，可能导致错误的复用                                    │
│                                                                             │
│  // 有 key                                                                  │
│  items.map(item => <Item key={item.id} data={item} />)                      │
│  // React 可以准确追踪每个元素                                              │
│                                                                             │
│  ⭐ key 存储位置: element.key                                               │
│  ⭐ key 不在 props 里！不能通过 props.key 访问                              │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│                       ref 字段                                               │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  作用: 获取 DOM 节点或组件实例的引用                                        │
│                                                                             │
│  // 使用 ref                                                                │
│  const inputRef = useRef(null);                                             │
│  <input ref={inputRef} />                                                   │
│  // inputRef.current === <input> DOM 节点                                   │
│                                                                             │
│  ⭐ ref 存储位置: element.ref                                               │
│  ⭐ ref 不在 props 里！（函数组件需要 forwardRef 才能接收 ref）             │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘

源码中的特殊处理:
─────────────────────────

📁 packages/react/src/ReactElement.js

const RESERVED_PROPS = {
  key: true,
  ref: true,
  __self: true,
  __source: true,
};

// 在 createElement 中:
for (propName in config) {
  if (
    hasOwnProperty.call(config, propName) &&
    !RESERVED_PROPS.hasOwnProperty(propName)  // ⭐ key/ref 被排除
  ) {
    props[propName] = config[propName];
  }
}

// key 和 ref 单独提取，不放入 props
if (hasValidKey(config)) {
  key = '' + config.key;
}
if (hasValidRef(config)) {
  ref = config.ref;
}
`;

/**
 * 📊 props 和 _owner 字段
 */

const propsAndOwnerExplanation = `
📊 props 和 _owner 字段详解

┌─────────────────────────────────────────────────────────────────────────────┐
│                       props 字段                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  props 包含：                                                               │
│  • 传递给组件的所有属性（除了 key 和 ref）                                  │
│  • children（子元素）                                                       │
│                                                                             │
│  示例:                                                                      │
│  <Button type="primary" disabled onClick={fn}>                              │
│    Click me                                                                 │
│  </Button>                                                                  │
│                                                                             │
│  生成的 props:                                                              │
│  {                                                                          │
│    type: "primary",                                                         │
│    disabled: true,                                                          │
│    onClick: fn,                                                             │
│    children: "Click me"  // ⭐ children 也在 props 里                       │
│  }                                                                          │
│                                                                             │
│  ⭐ props 在开发环境会被 Object.freeze() 冻结，防止意外修改                 │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│                       _owner 字段                                            │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  _owner 指向创建这个元素的组件（Fiber）                                     │
│                                                                             │
│  作用:                                                                      │
│  1. 错误边界定位                                                            │
│     • 当渲染出错时，React 可以追溯到哪个组件创建了问题元素                  │
│                                                                             │
│  2. String ref 解析（已废弃）                                               │
│     • 旧版本的 ref="inputRef" 需要找到 owner 来解析                         │
│                                                                             │
│  源码:                                                                      │
│  import ReactCurrentOwner from './ReactCurrentOwner';                       │
│                                                                             │
│  // 在创建元素时:                                                           │
│  return ReactElement(                                                       │
│    type,                                                                    │
│    key,                                                                     │
│    ref,                                                                     │
│    self,                                                                    │
│    source,                                                                  │
│    ReactCurrentOwner.current,  // ⭐ 当前正在渲染的组件 Fiber               │
│    props,                                                                   │
│  );                                                                         │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
`;

// ============================================================
// Part 3: createElement 的创建过程
// ============================================================

/**
 * 📊 createElement 源码解析
 */

const createElementExplanation = `
📊 React.createElement 源码解析

📁 源码位置: packages/react/src/ReactElement.js (Line 362-451)

函数签名:
─────────────────────────

createElement(type, config, ...children)

参数说明:
• type: 元素类型（'div'、组件函数、组件类）
• config: 属性对象（包含 key、ref 和其他 props）
• children: 子元素（可以有多个）


执行流程（伪代码）:
═══════════════════════════════════════════════════════════════════════════════

function createElement(type, config, children) {
  // ========== Step 1: 初始化变量 ==========
  const props = {};
  let key = null;
  let ref = null;
  let self = null;
  let source = null;

  // ========== Step 2: 处理 config ==========
  if (config != null) {
    // 2.1 提取 ref
    if (hasValidRef(config)) {
      ref = config.ref;
    }
    
    // 2.2 提取 key（转为字符串）
    if (hasValidKey(config)) {
      key = '' + config.key;
    }
    
    // 2.3 提取 DEV 专用字段
    self = config.__self;
    source = config.__source;
    
    // 2.4 复制其他属性到 props（排除保留字段）
    for (propName in config) {
      if (
        hasOwnProperty.call(config, propName) &&
        !RESERVED_PROPS[propName]  // 排除 key, ref, __self, __source
      ) {
        props[propName] = config[propName];
      }
    }
  }

  // ========== Step 3: 处理 children ==========
  const childrenLength = arguments.length - 2;
  
  if (childrenLength === 1) {
    // 单个子元素，直接赋值
    props.children = children;
  } else if (childrenLength > 1) {
    // 多个子元素，放入数组
    const childArray = [];
    for (let i = 0; i < childrenLength; i++) {
      childArray[i] = arguments[i + 2];
    }
    props.children = childArray;
  }

  // ========== Step 4: 处理 defaultProps ==========
  if (type && type.defaultProps) {
    const defaultProps = type.defaultProps;
    for (propName in defaultProps) {
      if (props[propName] === undefined) {
        props[propName] = defaultProps[propName];
      }
    }
  }

  // ========== Step 5: 创建并返回 ReactElement ==========
  return ReactElement(
    type,
    key,
    ref,
    self,
    source,
    ReactCurrentOwner.current,
    props,
  );
}
`;

// ============================================================
// Part 4: 真实案例详解
// ============================================================

/**
 * 📊 用例 A: 简单函数组件
 */

const caseASimpleComponent = `
📊 用例 A: 简单函数组件

JSX 代码:
─────────────────────────

function Hello({ name }) {
  return <h1>Hello, {name}!</h1>;
}

// 使用
<Hello name="React" />


编译后（Classic Runtime）:
─────────────────────────

React.createElement(Hello, { name: "React" })


编译后（Automatic Runtime）:
─────────────────────────

import { jsx as _jsx } from 'react/jsx-runtime';

_jsx(Hello, { name: "React" })


生成的 ReactElement:
─────────────────────────

{
  $$typeof: Symbol(react.element),
  type: Hello,                    // ⭐ 指向 Hello 函数本身
  key: null,
  ref: null,
  props: {
    name: "React"                 // ⭐ 传入的属性
  },
  _owner: null                    // 如果不在组件内创建则为 null
}


当 Hello 组件执行时:
─────────────────────────

// Hello({ name: "React" }) 返回:
{
  $$typeof: Symbol(react.element),
  type: "h1",                     // ⭐ 原生 DOM 类型
  key: null,
  ref: null,
  props: {
    children: ["Hello, ", "React", "!"]  // ⭐ 文本和变量混合
  },
  _owner: FiberNode(Hello)        // ⭐ 指向 Hello 组件的 Fiber
}
`;

/**
 * 📊 用例 B: 带 key 和 ref 的列表
 */

const caseBListWithKeyRef = `
📊 用例 B: 带 key 和 ref 的列表

JSX 代码:
─────────────────────────

function ItemList({ items }) {
  const firstItemRef = useRef(null);
  
  return (
    <ul>
      {items.map((item, index) => (
        <Item 
          key={item.id}
          ref={index === 0 ? firstItemRef : null}
          data={item}
        />
      ))}
    </ul>
  );
}


编译后（核心部分）:
─────────────────────────

items.map((item, index) => 
  _jsx(Item, {
    ref: index === 0 ? firstItemRef : null,
    data: item
  }, item.id)  // ⭐ key 作为第三个参数传入
)


单个 Item 生成的 ReactElement:
─────────────────────────

// 假设 item = { id: "123", name: "foo" }
{
  $$typeof: Symbol(react.element),
  type: Item,                     // Item 组件函数
  key: "123",                     // ⭐ key 单独存储，不在 props 里
  ref: { current: null },         // ⭐ ref 对象，也不在 props 里
  props: {
    data: { id: "123", name: "foo" }  // ⭐ 只有 data，没有 key 和 ref
  },
  _owner: FiberNode(ItemList)
}


key 和 ref 在后续阶段的使用:
═══════════════════════════════════════════════════════════════════════════════

Phase 2 (Fiber 架构):
┌─────────────────────────────────────────────────────────────────────────────┐
│                                                                             │
│   ReactElement.key  ──────▶  FiberNode.key                                 │
│                             用于在兄弟节点中唯一标识此 Fiber                │
│                                                                             │
│   ReactElement.ref  ──────▶  FiberNode.ref                                 │
│                             在 commit 阶段赋值给 ref.current                │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘

Phase 5 (Diff 算法):
┌─────────────────────────────────────────────────────────────────────────────┐
│                                                                             │
│   Diff 时的比较逻辑:                                                        │
│                                                                             │
│   1. 先比较 key                                                             │
│      - key 相同 → 可能是同一个节点，继续比较 type                           │
│      - key 不同 → 不是同一个节点                                            │
│                                                                             │
│   2. 再比较 type                                                            │
│      - key 和 type 都相同 → 复用节点，只更新 props                          │
│      - type 不同 → 销毁旧节点，创建新节点                                   │
│                                                                             │
│   // 简化的判断逻辑                                                         │
│   function canReuse(oldFiber, newElement) {                                 │
│     return (                                                                │
│       oldFiber.key === newElement.key &&                                    │
│       oldFiber.type === newElement.type                                     │
│     );                                                                      │
│   }                                                                         │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
`;

// ============================================================
// Part 5: 面试要点
// ============================================================

const interviewPoints = `
💡 Part 2 面试要点

Q1: ReactElement 有哪些字段？各有什么作用？
A: 核心字段：
   - $$typeof: Symbol 标识，用于安全校验和类型识别
   - type: 元素类型（字符串、函数、类、Symbol）
   - key: 列表 Diff 的标识
   - ref: DOM/组件实例引用
   - props: 属性对象（包含 children）
   - _owner: 创建此元素的组件 Fiber

Q2: $$typeof 为什么用 Symbol？有什么安全意义？
A: JSON 无法序列化 Symbol。
   如果攻击者通过接口注入恶意 JSON 数据，企图伪造 ReactElement，
   由于 JSON.parse 无法创建 Symbol，$$typeof 字段会丢失，
   React 的 isValidElement 检查会失败，阻止恶意内容渲染。

Q3: key 和 ref 为什么不在 props 里？
A: 它们是 React 内部使用的特殊属性：
   - key 用于 Diff 算法识别节点
   - ref 用于获取 DOM 或组件实例
   为了防止开发者误用（如 props.key），React 将它们从 props 中剥离，
   并在开发环境下对 props.key 的访问发出警告。

Q4: createElement 处理 children 的逻辑是什么？
A: - 单个子元素：直接赋值给 props.children
   - 多个子元素：收集到数组，赋值给 props.children
   - 无子元素：props.children 为 undefined

Q5: type 可以是哪些值？
A: - 字符串：原生 DOM 元素（'div'、'span'）
   - 函数：函数组件
   - 类：类组件
   - Symbol：Fragment、Suspense、StrictMode 等
   - 对象：Context.Provider、Context.Consumer、forwardRef 等
`;

export {
  reactElementStructure,
  typeofFieldExplanation,
  typeFieldExplanation,
  keyAndRefExplanation,
  propsAndOwnerExplanation,
  createElementExplanation,
  caseASimpleComponent,
  caseBListWithKeyRef,
  interviewPoints,
};

