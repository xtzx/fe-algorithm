/**
 * ============================================================
 * 📚 Phase 8: 事件系统 - Part 1: 概览与设计思想
 * ============================================================
 *
 * 📁 核心源码位置:
 * - packages/react-dom/src/events/DOMPluginEventSystem.js
 * - packages/react-dom/src/events/ReactDOMEventListener.js
 * - packages/react-dom/src/events/SyntheticEvent.js
 *
 * ⏱️ 预计时间：3-4 小时
 * 🎯 面试权重：⭐⭐⭐⭐
 */

// ============================================================
// Part 1: 为什么 React 需要自己的事件系统
// ============================================================

/**
 * 📊 设计动机
 */

const designMotivation = `
📊 为什么 React 需要自己的事件系统？

┌─────────────────────────────────────────────────────────────────────────────┐
│                        原生事件的问题                                        │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  1. 跨浏览器兼容性问题                                                       │
│     - IE vs Chrome vs Safari 事件属性不一致                                  │
│     - 例如: event.target vs event.srcElement                                │
│     - 例如: event.which vs event.keyCode                                    │
│                                                                             │
│  2. 性能问题                                                                 │
│     - 每个 DOM 节点都绑定事件监听器 → 内存占用高                              │
│     - 动态增删节点需要手动管理监听器                                         │
│                                                                             │
│  3. 与 React 渲染流程脱节                                                    │
│     - 原生事件无法感知 React 的优先级系统                                    │
│     - 无法与 Scheduler 配合                                                  │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│                        React 事件系统的解决方案                              │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  1. 跨浏览器统一（SyntheticEvent）                                           │
│     - 统一的事件对象接口                                                     │
│     - 抹平浏览器差异                                                         │
│     - 实现 W3C 标准 API                                                      │
│                                                                             │
│  2. 事件委托（Event Delegation）                                             │
│     - 所有事件统一绑定到根节点（root container）                             │
│     - 利用事件冒泡机制                                                       │
│     - 大幅减少内存占用                                                       │
│                                                                             │
│  3. 与 React 渲染集成                                                        │
│     - 事件触发的更新有明确优先级                                             │
│     - 离散事件 vs 连续事件 不同处理                                          │
│     - 与 Scheduler 和 Lane 模型配合                                          │
│                                                                             │
│  4. 插件化架构                                                               │
│     - 不同类型事件由不同插件处理                                             │
│     - 便于扩展和维护                                                         │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
`;

// ============================================================
// Part 2: 事件系统核心特性
// ============================================================

/**
 * 📊 核心特性总览
 */

const coreFeatures = `
📊 React 事件系统核心特性

┌─────────────────────────────────────────────────────────────────────────────┐
│ 特性                        │ 说明                                          │
├─────────────────────────────┼───────────────────────────────────────────────┤
│                             │                                               │
│ SyntheticEvent 合成事件     │ 跨浏览器统一的事件对象                        │
│                             │ 包装原生事件，提供一致的 API                  │
│                             │                                               │
│ 事件委托 (Delegation)       │ 事件监听器绑定在 root container                │
│                             │ 不是绑定在每个 DOM 元素上                     │
│                             │                                               │
│ 事件优先级                  │ DiscreteEvent > ContinuousEvent > Default     │
│                             │ 影响更新的调度优先级                          │
│                             │                                               │
│ 插件化架构                  │ SimpleEventPlugin、ChangeEventPlugin 等      │
│                             │ 不同插件处理不同类型事件                      │
│                             │                                               │
│ 捕获/冒泡模拟               │ onClickCapture / onClick                      │
│                             │ 在 React 层面模拟原生事件流                   │
│                             │                                               │
└─────────────────────────────┴───────────────────────────────────────────────┘
`;

// ============================================================
// Part 3: React 17+ 现代事件系统的变化
// ============================================================

/**
 * 📊 React 17 事件系统的重要改变
 */

const react17Changes = `
📊 React 17+ 现代事件系统的变化

┌─────────────────────────────────────────────────────────────────────────────┐
│                        React 16 及之前                                       │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  事件委托目标: document                                                      │
│                                                                             │
│                     document ← 所有 React 事件都委托到这里                  │
│                        │                                                    │
│                        ▼                                                    │
│                     <html>                                                  │
│                        │                                                    │
│                        ▼                                                    │
│                     <body>                                                  │
│                        │                                                    │
│                        ▼                                                    │
│               ┌────────────────┐                                            │
│               │  #root         │  ← React 根节点                            │
│               │   └── App      │                                            │
│               │       └── ...  │                                            │
│               └────────────────┘                                            │
│                                                                             │
│  问题:                                                                      │
│  1. 多个 React 版本在同一页面会冲突                                          │
│  2. 和其他框架（jQuery 等）的事件可能互相干扰                                │
│  3. 微前端场景下难以隔离                                                    │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│                        React 17+ (现代事件系统)                              │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  事件委托目标: root container                                                │
│                                                                             │
│                     document                                                │
│                        │                                                    │
│                        ▼                                                    │
│                     <html>                                                  │
│                        │                                                    │
│                        ▼                                                    │
│                     <body>                                                  │
│                        │                                                    │
│                        ▼                                                    │
│               ┌────────────────┐                                            │
│               │  #root         │  ← ⭐ React 事件委托到这里                 │
│               │   └── App      │                                            │
│               │       └── ...  │                                            │
│               └────────────────┘                                            │
│                                                                             │
│  优势:                                                                      │
│  1. 多个 React 应用可以共存，事件互不干扰                                    │
│  2. 微前端友好                                                              │
│  3. 渐进式升级更容易                                                        │
│  4. 更符合直觉的事件冒泡行为                                                │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘

其他重要变化:
─────────────────────────────────────

1. 事件池化（Event Pooling）被移除
   - React 16: 事件对象会被复用，异步访问需要 e.persist()
   - React 17+: 每次都创建新的事件对象，无需 persist()

2. onScroll 事件不再冒泡
   - 更符合原生行为

3. onFocus/onBlur 使用原生 focusin/focusout
   - 在捕获阶段处理

4. 原生捕获阶段的事件监听
   - onClickCapture 真正在捕获阶段触发
`;

// ============================================================
// Part 4: 事件系统架构图
// ============================================================

/**
 * 📊 事件系统整体架构
 */

const eventSystemArchitecture = `
📊 React 事件系统架构

┌─────────────────────────────────────────────────────────────────────────────┐
│                                                                             │
│                           用户交互                                          │
│                              │                                              │
│                              ▼                                              │
│  ┌───────────────────────────────────────────────────────────────────────┐ │
│  │                       原生 DOM 事件                                    │ │
│  │                                                                       │ │
│  │   用户点击 <button> → 原生 click 事件产生 → 冒泡到 root container     │ │
│  └───────────────────────────────────────────────────────────────────────┘ │
│                              │                                              │
│                              ▼                                              │
│  ┌───────────────────────────────────────────────────────────────────────┐ │
│  │                    ReactDOMEventListener                              │ │
│  │                                                                       │ │
│  │   📁 ReactDOMEventListener.js                                        │ │
│  │                                                                       │ │
│  │   1. 接收原生事件                                                     │ │
│  │   2. 根据事件类型确定优先级                                           │ │
│  │   3. 设置当前更新优先级                                               │ │
│  │   4. 调用 dispatchEvent                                               │ │
│  └───────────────────────────────────────────────────────────────────────┘ │
│                              │                                              │
│                              ▼                                              │
│  ┌───────────────────────────────────────────────────────────────────────┐ │
│  │                    DOMPluginEventSystem                               │ │
│  │                                                                       │ │
│  │   📁 DOMPluginEventSystem.js                                         │ │
│  │                                                                       │ │
│  │   1. 查找目标 Fiber (getInstanceFromNode)                            │ │
│  │   2. 调用各插件的 extractEvents                                       │ │
│  │   3. 收集 dispatch queue                                              │ │
│  │   4. 处理 dispatch queue                                              │ │
│  └───────────────────────────────────────────────────────────────────────┘ │
│                              │                                              │
│                              ▼                                              │
│  ┌───────────────────────────────────────────────────────────────────────┐ │
│  │                     Event Plugins                                     │ │
│  │                                                                       │ │
│  │   📁 plugins/SimpleEventPlugin.js    → click, keydown 等基本事件     │ │
│  │   📁 plugins/ChangeEventPlugin.js    → onChange 统一处理             │ │
│  │   📁 plugins/EnterLeaveEventPlugin.js → mouseenter/leave 处理        │ │
│  │   📁 plugins/SelectEventPlugin.js    → onSelect 处理                 │ │
│  │   📁 plugins/BeforeInputEventPlugin.js → beforeInput 处理            │ │
│  │                                                                       │ │
│  │   每个插件:                                                           │ │
│  │   - 决定是否处理该事件                                                │ │
│  │   - 创建 SyntheticEvent                                               │ │
│  │   - 收集 listeners (向上遍历 Fiber 树)                                │ │
│  └───────────────────────────────────────────────────────────────────────┘ │
│                              │                                              │
│                              ▼                                              │
│  ┌───────────────────────────────────────────────────────────────────────┐ │
│  │                    SyntheticEvent                                     │ │
│  │                                                                       │ │
│  │   📁 SyntheticEvent.js                                               │ │
│  │                                                                       │ │
│  │   - 包装原生事件                                                      │ │
│  │   - 提供跨浏览器统一的 API                                            │ │
│  │   - preventDefault() / stopPropagation()                              │ │
│  └───────────────────────────────────────────────────────────────────────┘ │
│                              │                                              │
│                              ▼                                              │
│  ┌───────────────────────────────────────────────────────────────────────┐ │
│  │                    组件事件处理函数                                    │ │
│  │                                                                       │ │
│  │   function handleClick(e) {                                           │ │
│  │     // e 是 SyntheticEvent                                            │ │
│  │     console.log(e.target);                                            │ │
│  │     setState(...);  // 触发更新                                       │ │
│  │   }                                                                   │ │
│  └───────────────────────────────────────────────────────────────────────┘ │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
`;

// ============================================================
// Part 5: 事件命名映射
// ============================================================

/**
 * 📊 原生事件 → React 事件映射
 */

const eventNameMapping = `
📊 原生事件名 → React 事件名 映射

┌─────────────────────────────────────────────────────────────────────────────┐
│ 原生事件名            │ React 事件名           │ 类型                       │
├───────────────────────┼────────────────────────┼────────────────────────────┤
│ click                 │ onClick                │ 离散事件（高优先级）       │
│ dblclick              │ onDoubleClick          │ 离散事件                   │
│ mousedown             │ onMouseDown            │ 离散事件                   │
│ mouseup               │ onMouseUp              │ 离散事件                   │
│ keydown               │ onKeyDown              │ 离散事件                   │
│ keyup                 │ onKeyUp                │ 离散事件                   │
│ input                 │ onChange*              │ 离散事件                   │
├───────────────────────┼────────────────────────┼────────────────────────────┤
│ mousemove             │ onMouseMove            │ 连续事件（较低优先级）     │
│ mouseenter            │ onMouseEnter           │ 连续事件                   │
│ mouseleave            │ onMouseLeave           │ 连续事件                   │
│ scroll                │ onScroll               │ 连续事件                   │
│ wheel                 │ onWheel                │ 连续事件                   │
│ pointermove           │ onPointerMove          │ 连续事件                   │
├───────────────────────┼────────────────────────┼────────────────────────────┤
│ load                  │ onLoad                 │ 默认优先级                 │
│ error                 │ onError                │ 默认优先级                 │
│ animation*            │ onAnimation*           │ 默认优先级                 │
│ transition*           │ onTransition*          │ 默认优先级                 │
└───────────────────────┴────────────────────────┴────────────────────────────┘

* onChange 特殊说明：
  React 的 onChange 不等于原生的 change 事件
  它实际上监听的是 input、keydown、compositionend 等多个事件
  目的是提供一致的"值改变"语义

📁 事件名映射源码: packages/react-dom/src/events/DOMEventProperties.js
`;

// ============================================================
// Part 6: 面试要点
// ============================================================

const interviewPoints = `
💡 Part 1 面试要点

Q1: React 为什么要实现自己的事件系统？
A: 三个主要原因：
   1. 跨浏览器兼容：SyntheticEvent 抹平浏览器差异
   2. 性能优化：事件委托减少内存占用
   3. 与 React 渲染集成：事件优先级影响更新调度

Q2: React 17 事件系统有什么重大变化？
A:
   1. 事件委托从 document 改为 root container
   2. 移除事件池化（Event Pooling）
   3. onScroll 不再冒泡
   4. 原生捕获阶段事件真正在捕获阶段触发

Q3: 什么是事件委托？React 如何实现？
A: 事件委托是把事件监听器绑定在父元素上，利用事件冒泡处理子元素事件。
   React 在 createRoot 时，调用 listenToAllSupportedEvents
   在 root container 上注册所有支持的事件监听器。
   当事件触发时，通过 event.target 找到对应的 Fiber。

Q4: 离散事件和连续事件有什么区别？
A:
   离散事件（click、keydown）：用户明确的单次交互，高优先级
   连续事件（mousemove、scroll）：用户持续的交互，较低优先级
   这影响了事件触发更新时的调度优先级（Lane）

Q5: SyntheticEvent 和原生事件有什么关系？
A: SyntheticEvent 包装了原生事件（nativeEvent 属性）
   提供统一的 API 接口
   实现了 W3C 标准（preventDefault、stopPropagation 等）
   在 React 17+ 中不再复用，每次都是新对象
`;

export {
  designMotivation,
  coreFeatures,
  react17Changes,
  eventSystemArchitecture,
  eventNameMapping,
  interviewPoints,
};

