/**
 * ============================================================
 * 📚 Step 06: 二分查找
 * ============================================================
 *
 * 本章内容：
 * 1. 二分查找基础
 * 2. 标准二分查找
 * 3. 左边界二分（第一个 >= target）
 * 4. 右边界二分（最后一个 <= target）
 * 5. 旋转数组的二分
 * 6. 搜索空间的二分（答案二分）
 */

// ============================================================
// 1. 二分查找基础
// ============================================================

/**
 * 📖 二分查找的本质：
 *
 * 不只是在有序数组中查找元素！
 *
 * 更本质的理解：
 * - 有一个判断条件，把搜索空间分成两部分
 * - 左边全部满足/不满足，右边全部不满足/满足
 * - 找到这个分界点
 *
 * ┌─────────────────────────────────────────────────────┐
 * │  F   F   F   F   T   T   T   T   T   T   T          │
 * │  ←───不满足───→   ←──────满足──────→               │
 * │              ↑                                      │
 * │            分界点                                    │
 * └─────────────────────────────────────────────────────┘
 *
 * 二分查找的前提：
 * - 搜索空间具有单调性（或二段性）
 * - 可以通过判断条件排除一半的搜索空间
 *
 * 时间复杂度：O(log n)
 * 空间复杂度：O(1)
 */

// ============================================================
// 2. 🎯 二分查找 - 适用场景与信号词
// ============================================================

/**
 * 🎯 信号词识别：
 *
 * ┌────────────────────────────────────────────────────────────┐
 * │                   二分查找问题分类                          │
 * ├────────────────────────────────────────────────────────────┤
 * │                                                            │
 * │  【有序数组查找】                                           │
 * │   信号词: 有序数组、查找、搜索                              │
 * │   典型题: 二分查找、搜索插入位置                            │
 * │                                                            │
 * │  【边界查找】                                               │
 * │   信号词: 第一个、最后一个、左边界、右边界                  │
 * │   典型题: 在排序数组中查找元素的第一个和最后一个位置        │
 * │                                                            │
 * │  【旋转数组】                                               │
 * │   信号词: 旋转、循环、部分有序                              │
 * │   典型题: 搜索旋转排序数组、寻找旋转数组的最小值            │
 * │                                                            │
 * │  【答案二分】                                               │
 * │   信号词: 最大化最小、最小化最大、能否完成                  │
 * │   典型题: 分割数组的最大值、爱吃香蕉的珂珂                  │
 * │                                                            │
 * │  【矩阵二分】                                               │
 * │   信号词: 有序矩阵、搜索矩阵                                │
 * │   典型题: 搜索二维矩阵、有序矩阵中第K小的元素              │
 * │                                                            │
 * └────────────────────────────────────────────────────────────┘
 */

// ============================================================
// 3. 代码模板
// ============================================================

// ────────────────────────────────────────────────────────────
// 模板一：标准二分查找（查找确切值）
// ────────────────────────────────────────────────────────────

/**
 * 📊 标准二分查找图解：
 *
 * 数组: [1, 3, 5, 7, 9, 11]
 * 查找: 7
 *
 *  Step 1:  left=0, right=5, mid=2
 *           nums[2]=5 < 7, left=3
 *
 *  Step 2:  left=3, right=5, mid=4
 *           nums[4]=9 > 7, right=3
 *
 *  Step 3:  left=3, right=3, mid=3
 *           nums[3]=7 == 7, 找到！
 *
 * 🔄 流程图 (Mermaid):
 * ```mermaid
 * flowchart TD
 *     A[left=0, right=n-1] --> B{left <= right?}
 *     B -->|Yes| C[mid = left + right >> 1]
 *     C --> D{nums mid == target?}
 *     D -->|Yes| E[返回 mid]
 *     D -->|No| F{nums mid < target?}
 *     F -->|Yes| G[left = mid + 1]
 *     F -->|No| H[right = mid - 1]
 *     G --> B
 *     H --> B
 *     B -->|No| I[返回 -1]
 * ```
 */
function binarySearch(nums: number[], target: number): number {
  let left = 0;
  let right = nums.length - 1;

  while (left <= right) {
    const mid = left + ((right - left) >> 1); // 防止溢出
    if (nums[mid] === target) {
      return mid;
    } else if (nums[mid] < target) {
      left = mid + 1;
    } else {
      right = mid - 1;
    }
  }

  return -1;
}

// ────────────────────────────────────────────────────────────
// 模板二：左边界二分（第一个 >= target 的位置）
// ────────────────────────────────────────────────────────────

/**
 * 📊 左边界二分图解：
 *
 * 数组: [1, 2, 2, 2, 3, 4]
 * 查找: 第一个 >= 2 的位置
 *
 * ┌───┬───┬───┬───┬───┬───┐
 * │ 1 │ 2 │ 2 │ 2 │ 3 │ 4 │
 * └───┴───┴───┴───┴───┴───┘
 *       ↑
 *      答案 = 1
 *
 * 关键：找到满足条件的最左边位置
 * - 当 nums[mid] >= target 时，答案可能是 mid，但左边可能还有
 * - 所以 right = mid，继续在左边找
 */
function lowerBound(nums: number[], target: number): number {
  let left = 0;
  let right = nums.length; // 注意：right 初始为 n

  while (left < right) {
    // 注意：不是 <=
    const mid = left + ((right - left) >> 1);
    if (nums[mid] >= target) {
      right = mid; // mid 可能是答案，所以不能 mid - 1
    } else {
      left = mid + 1;
    }
  }

  return left; // left == right
}

// ────────────────────────────────────────────────────────────
// 模板三：右边界二分（最后一个 <= target 的位置）
// ────────────────────────────────────────────────────────────

/**
 * 📊 右边界二分图解：
 *
 * 数组: [1, 2, 2, 2, 3, 4]
 * 查找: 最后一个 <= 2 的位置
 *
 * ┌───┬───┬───┬───┬───┬───┐
 * │ 1 │ 2 │ 2 │ 2 │ 3 │ 4 │
 * └───┴───┴───┴───┴───┴───┘
 *             ↑
 *           答案 = 3
 *
 * 技巧：转化为找第一个 > target 的位置，然后 -1
 */
function upperBound(nums: number[], target: number): number {
  let left = 0;
  let right = nums.length;

  while (left < right) {
    const mid = left + ((right - left) >> 1);
    if (nums[mid] > target) {
      right = mid;
    } else {
      left = mid + 1;
    }
  }

  return left - 1; // 返回最后一个 <= target 的位置
}

// ────────────────────────────────────────────────────────────
// 模板四：旋转数组二分
// ────────────────────────────────────────────────────────────

/**
 * 📊 旋转数组图解：
 *
 * 原数组:   [1, 2, 3, 4, 5, 6, 7]
 * 旋转后:   [4, 5, 6, 7, 1, 2, 3]
 *                    ↑
 *                  旋转点
 *
 * 特点：
 * - 旋转点左边递增，右边递增
 * - 左边的最小值 > 右边的最大值
 *
 * 判断 mid 在左半还是右半：
 * - nums[mid] > nums[right] → mid 在左半段
 * - nums[mid] < nums[right] → mid 在右半段
 */
function searchRotated(nums: number[], target: number): number {
  let left = 0;
  let right = nums.length - 1;

  while (left <= right) {
    const mid = left + ((right - left) >> 1);

    if (nums[mid] === target) {
      return mid;
    }

    // 判断哪半边是有序的
    if (nums[left] <= nums[mid]) {
      // 左半边有序
      if (nums[left] <= target && target < nums[mid]) {
        right = mid - 1;
      } else {
        left = mid + 1;
      }
    } else {
      // 右半边有序
      if (nums[mid] < target && target <= nums[right]) {
        left = mid + 1;
      } else {
        right = mid - 1;
      }
    }
  }

  return -1;
}

// ────────────────────────────────────────────────────────────
// 模板五：答案二分（搜索空间二分）
// ────────────────────────────────────────────────────────────

/**
 * 📊 答案二分思路：
 *
 * 问题：能否在 x 条件下完成任务？
 *
 * 如果答案具有单调性：
 * - x 太小 → 完不成
 * - x 足够大 → 能完成
 *
 * ┌──────────────────────────────────────────────┐
 * │  不能完成 不能完成 能完成 能完成 能完成 ...   │
 * │  ←──────────→ ←────────────────────→        │
 * │              ↑                               │
 * │       找第一个能完成的 x                     │
 * └──────────────────────────────────────────────┘
 *
 * 步骤：
 * 1. 确定搜索空间 [min, max]
 * 2. 写一个 check(x) 函数判断 x 是否可行
 * 3. 二分找第一个可行的 x
 */

/**
 * 🌰 例子：爱吃香蕉的珂珂
 *
 * 有 n 堆香蕉，每堆 piles[i] 个
 * 珂珂每小时吃 k 个（一堆吃不完也算一小时）
 * 在 h 小时内吃完，求最小的 k
 *
 * check(k): 以速度 k 能否在 h 小时内吃完
 * - 每堆需要 ceil(piles[i] / k) 小时
 * - 总时间 <= h 就能完成
 */
function minEatingSpeed(piles: number[], h: number): number {
  let left = 1;
  let right = Math.max(...piles);

  while (left < right) {
    const mid = left + ((right - left) >> 1);
    if (canFinish(piles, h, mid)) {
      right = mid; // mid 可行，尝试更小的
    } else {
      left = mid + 1; // mid 不可行，需要更大的
    }
  }

  return left;
}

function canFinish(piles: number[], h: number, k: number): boolean {
  let hours = 0;
  for (const pile of piles) {
    hours += Math.ceil(pile / k);
  }
  return hours <= h;
}

// ============================================================
// 4. 三种模板对比
// ============================================================

/**
 * ┌─────────────┬───────────────────┬─────────────────────────────────┐
 * │   模板       │    循环条件        │           适用场景              │
 * ├─────────────┼───────────────────┼─────────────────────────────────┤
 * │ left <= right │ left <= right   │ 查找确切值，找到就返回           │
 * │ left < right  │ left < right    │ 找边界，最后 left==right        │
 * │ 答案二分      │ left < right    │ 搜索空间二分，判断可行性         │
 * └─────────────┴───────────────────┴─────────────────────────────────┘
 *
 * 边界处理：
 * ┌─────────────┬───────────────────┬─────────────────────┐
 * │   场景       │      right 更新   │    left 更新        │
 * ├─────────────┼───────────────────┼─────────────────────┤
 * │ 查找确切值   │   right = mid - 1 │  left = mid + 1    │
 * │ 找左边界    │   right = mid     │  left = mid + 1    │
 * │ 找右边界    │   right = mid     │  left = mid + 1    │
 * └─────────────┴───────────────────┴─────────────────────┘
 */

// ============================================================
// 5. 复杂度分析
// ============================================================

/**
 * ┌─────────────────────┬────────────┬────────────┬──────────────────────┐
 * │       操作          │ 时间复杂度  │ 空间复杂度  │       说明           │
 * ├─────────────────────┼────────────┼────────────┼──────────────────────┤
 * │ 标准二分            │ O(log n)   │   O(1)     │ 每次排除一半          │
 * │ 边界二分            │ O(log n)   │   O(1)     │ 同上                 │
 * │ 答案二分            │ O(log n ×  │   O(1)     │ check() 可能需要     │
 * │                    │  check())   │           │ O(n) 时间            │
 * │ 旋转数组            │ O(log n)   │   O(1)     │ 每次排除一半          │
 * └─────────────────────┴────────────┴────────────┴──────────────────────┘
 */

// ============================================================
// 6. ⚠️ 易错点
// ============================================================

/**
 * ┌─────────────────────────────────────────────────────────────┐
 * │                      易错点总结                              │
 * ├─────────────────────────────────────────────────────────────┤
 * │                                                             │
 * │ 【循环条件】                                                 │
 * │  ⚠️ left <= right vs left < right 要分清                   │
 * │  ⚠️ 查找确切值用 <=，找边界用 <                             │
 * │                                                             │
 * │ 【mid 计算】                                                 │
 * │  ⚠️ mid = left + (right - left) / 2 防止溢出               │
 * │  ⚠️ 或者用位运算 (right - left) >> 1                       │
 * │                                                             │
 * │ 【边界更新】                                                 │
 * │  ⚠️ right = mid 还是 mid - 1？                              │
 * │  ⚠️ 取决于 mid 是否可能是答案                               │
 * │                                                             │
 * │ 【死循环】                                                   │
 * │  ⚠️ left < right 时，如果 left = mid 可能死循环             │
 * │  ⚠️ 因为 left == right - 1 时，mid = left，left 不变       │
 * │                                                             │
 * │ 【返回值】                                                   │
 * │  ⚠️ 找边界时返回 left 还是 right？（相等时）                │
 * │  ⚠️ 没找到时返回什么？                                      │
 * │                                                             │
 * │ 【旋转数组】                                                 │
 * │  ⚠️ 判断有序半边时注意等号                                  │
 * │  ⚠️ nums[left] <= nums[mid] 而不是 <                        │
 * │                                                             │
 * └─────────────────────────────────────────────────────────────┘
 */

// ============================================================
// 7. 🏢 前端业务场景
// ============================================================

/**
 * ┌─────────────────────────────────────────────────────────────┐
 * │                   前端业务场景应用                           │
 * ├─────────────────────────────────────────────────────────────┤
 * │                                                             │
 * │ 【虚拟列表】                                                 │
 * │  • 计算可视区域的起始和结束索引                             │
 * │  • 根据滚动位置二分查找                                     │
 * │                                                             │
 * │ 【时间选择器】                                               │
 * │  • 在时间刻度上定位                                         │
 * │  • 找最近的有效时间点                                       │
 * │                                                             │
 * │ 【版本比较】                                                 │
 * │  • Git bisect 查找引入 bug 的提交                           │
 * │  • 典型的答案二分应用                                       │
 * │                                                             │
 * │ 【IP 地址定位】                                              │
 * │  • 根据 IP 范围二分查找归属地                               │
 * │                                                             │
 * │ 【图表交互】                                                 │
 * │  • 找最近的数据点                                           │
 * │  • 鼠标位置对应的 X 轴值                                    │
 * │                                                             │
 * │ 【搜索建议】                                                 │
 * │  • 在有序词典中快速定位                                     │
 * │  • 自动补全的前缀匹配                                       │
 * │                                                             │
 * └─────────────────────────────────────────────────────────────┘
 */

export {
  binarySearch,
  lowerBound,
  upperBound,
  searchRotated,
  minEatingSpeed,
};

